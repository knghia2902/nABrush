---
phase: 03-core-annotation-tools
plan: 10
type: execute
wave: 10
depends_on: [03-09]
files_modified:
  - .github/workflows/phase3-native.yml
  - .planning/phases/03-core-annotation-tools/windows-evidence-gate.sh
  - .planning/phases/03-core-annotation-tools/windows-phase3-run.json
  - .planning/phases/03-core-annotation-tools/windows-phase3-tools-evidence/host-metadata.json
  - .planning/phases/03-core-annotation-tools/windows-phase3-tools-evidence/timestamps.json
  - .planning/phases/03-core-annotation-tools/windows-phase3-tools-evidence/phase3-tools.log
  - .planning/phases/03-core-annotation-tools/windows-phase3-tools-evidence/phase3-tools-results.json
  - .planning/phases/03-core-annotation-tools/windows-phase3-tools-evidence/native.log
  - .planning/phases/03-core-annotation-tools/03-VALIDATION.md
  - .planning/phases/03-core-annotation-tools/03-UAT.md
autonomous: false
gap_closure: true
gap_ids: [G-03-21]
requirements: [DRAW-01, DRAW-02, DRAW-03, DRAW-04, DRAW-05, DRAW-06]
estimate:
  tokens: 16000
  raw_tokens: 13000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A Windows-only workflow/job named phase3-tools runs the exact full Phase 3 setup and native command on a real Windows host."
    - "The Windows evidence bundle contains exactly host-metadata.json, timestamps.json, phase3-tools.log, phase3-tools-results.json, and native.log with host identity, UTC timing, command/exit status, generated-overlay identity, and all six parity groups."
    - "The automated Windows evidence gate scans every scoped Windows validation row, retained result/log/evidence file, generated-overlay evidence, and UAT gap/test/summary status, rejecting NOT RUN, PENDING, FAIL, SKIPPED, and macOS-inferred wording anywhere in those sources."
    - "Windows validation remains separate from macOS; G-03-21 closes only after a green real-host run with no skipped parity group and blocking human approval."
  artifacts:
    - ".github/workflows/phase3-native.yml with a Windows-only phase3-tools job, exact command, exact artifact names, six-group completeness, and fail-closed guards."
    - ".planning/phases/03-core-annotation-tools/windows-evidence-gate.sh implementing the source-wide Windows evidence scan."
    - "03-VALIDATION.md, 03-UAT.md, windows-phase3-run.json, and the retained evidence directory carrying separate Windows host evidence."
  key_links:
    - "03-09 generated-overlay target/readiness contract -> Windows phase3-tools job -> retained evidence bundle -> source-wide evidence gate -> Windows validation/UAT status."
    - "Windows-only guard + six-group completeness -> no skipped or macOS-inferred result can satisfy G-03-21."
---

<objective>
Close G-03-21 with an exact, fail-closed Windows workflow/evidence gate and a blocking real-host execution that updates validation and UAT only from retained Windows evidence.

Purpose: The existing record has no independent Windows runtime result, and shallow keyword checks could incorrectly pass while rows or retained artifacts still say incomplete or infer Windows from macOS.
Output: A Windows-only phase3-tools job, source-wide evidence gate, retained artifact contract, and host-observed UAT/validation update.
</objective>

<execution_context>
@/Users/khacnghia/.codex/gsd-core/workflows/execute-plan.md
@/Users/khacnghia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-annotation-tools/03-CONTEXT.md
@.planning/phases/03-core-annotation-tools/03-RESEARCH.md
@.planning/phases/03-core-annotation-tools/03-PATTERNS.md
@.planning/phases/03-core-annotation-tools/03-UAT.md
@.planning/phases/03-core-annotation-tools/03-VALIDATION.md
@.planning/phases/03-core-annotation-tools/03-VERIFICATION.md
@.planning/debug/g-03-21-windows-parity.md
@.planning/phases/03-core-annotation-tools/03-09-PLAN.md
</context>

<tasks>
<task type="tracer">
  <name>Task 03-10-01: Add the exact Windows phase3-tools workflow and retained evidence contract</name>
  <read_first>
    - `.planning/phases/03-core-annotation-tools/03-PATTERNS.md` — platform-separated evidence and generated-window discovery; consume 03-09's generated overlay-display-* contract.
    - `.planning/phases/03-core-annotation-tools/03-09-PLAN.md` — upstream bridge/readiness and target contract.
    - `.planning/debug/g-03-21-windows-parity.md` — absent Windows job/artifacts and why cross-compilation is not evidence.
    - `.planning/phases/03-core-annotation-tools/03-VALIDATION.md` and `.github/workflows/phase1.yml` — exact Windows command sequence and existing runner conventions.
  </read_first>
  <behavior>
    - The workflow contains a Windows-only job with job id/name exactly `phase3-tools` and a Windows runner guard that fails closed on a non-Windows host.
    - The job executes, in order, `pnpm install --frozen-lockfile`, `pnpm test`, `cargo test --manifest-path src-tauri/Cargo.toml`, `pnpm build`, `pnpm exec tauri build --debug`, and exactly `pnpm exec wdio run wdio.conf.ts --suite phase3-tools`.
    - The job verifies every required parity group: `pointer input`, `text/IME`, `eraser`, `click-through`, `style`, and `lifecycle`; a skipped, missing, or unreported group fails the job.
    - The job retains exactly named artifacts `host-metadata.json`, `timestamps.json`, `phase3-tools.log`, `phase3-tools-results.json`, and `native.log`, uploading them even when a command fails.
  </behavior>
  <files>.github/workflows/phase3-native.yml</files>
  <action>Define a dedicated Windows workflow/job named exactly `phase3-tools`. Capture OS/build, runner/device, UTC start/end, revision, and WebView/runtime metadata; run the exact frozen-install/test/Rust/build/debug-build/phase3-tools sequence; and enforce a Windows-only guard. Capture output and exit status into the exact five artifact names. Assert all six parity groups and generated `overlay-display-*` targeting; fail closed for skipped or missing groups and upload the bundle on success or failure. Keep macOS separate and do not treat cross-target compilation as Windows evidence.</action>
  <verify>
    <automated>test -f .github/workflows/phase3-native.yml &amp;&amp; rg -n 'phase3-tools|windows-2022|windows-latest|pnpm install --frozen-lockfile|pnpm test|cargo test --manifest-path src-tauri/Cargo.toml|pnpm build|pnpm exec tauri build --debug|pnpm exec wdio run wdio.conf.ts --suite phase3-tools|host-metadata\.json|timestamps\.json|phase3-tools\.log|phase3-tools-results\.json|native\.log|pointer input|text/IME|eraser|click-through|style|lifecycle|overlay-display-' .github/workflows/phase3-native.yml</automated>
    <fails_when>Any required workflow/job name, Windows-only guard, exact command, exact artifact name, all-six-group check, generated-overlay check, or always-upload behavior is absent; the command exits non-zero; or the workflow permits non-Windows execution or skipped groups to pass.</fails_when>
  </verify>
  <acceptance_criteria>The workflow has an exact Windows phase3-tools job, exact command/artifact/group contract, Windows-only guard, generated-overlay targeting check, and fail-closed skipped-group behavior.</acceptance_criteria>
  <done>The Windows execution path is identifiable and produces inputs required by the source-wide gate; G-03-21 remains open until the human run.</done>
</task>

<task type="auto">
  <name>Task 03-10-02: Implement the source-wide Windows evidence gate and validation/UAT handoff</name>
  <read_first>
    - `.planning/phases/03-core-annotation-tools/03-PATTERNS.md` — host-labeled evidence and generated-window discovery; document the intentional deviation from static overlay-label analogs.
    - `.planning/phases/03-core-annotation-tools/03-UAT.md` — Test 21, all gaps, and summary/status fields that remain truthful until execution.
    - `.planning/phases/03-core-annotation-tools/03-VALIDATION.md` — Windows row, six manual rows, sign-off fields, and evidence rules.
    - `.planning/debug/g-03-21-windows-parity.md` and `.planning/phases/03-core-annotation-tools/03-09-PLAN.md` — absent evidence, prohibited macOS inference, and upstream target contract.
  </read_first>
  <behavior>
    - The gate scans every scoped source, not only Test 21: all Windows validation rows, every retained Windows result/log/evidence file, generated-overlay evidence, and UAT gap/test/summary status.
    - The gate rejects any occurrence of `NOT RUN`, `PENDING`, `FAIL`, `SKIPPED`, or wording that infers Windows from macOS in those scoped sources; it also rejects missing/duplicate rows, wrong platform labels, absent generated overlay evidence, nonzero recorded exit status, or incomplete groups.
    - The gate requires exact artifact names `host-metadata.json`, `timestamps.json`, `phase3-tools.log`, `phase3-tools-results.json`, and `native.log`, exact command/job identity, Windows metadata, UTC timestamps, and all six parity groups.
    - The gate keeps G-03-21 open when evidence is unavailable; configuration inspection alone cannot mark UAT pass.
  </behavior>
  <files>.planning/phases/03-core-annotation-tools/windows-evidence-gate.sh, .planning/phases/03-core-annotation-tools/03-VALIDATION.md, .planning/phases/03-core-annotation-tools/03-UAT.md</files>
  <action>Write an executable gate with an explicit scoped-file list covering the Windows validation rows, retained evidence directory, generated-overlay evidence block, Windows run record, and UAT gap/Test 21/summary status. Scan the complete contents of every listed source for forbidden incomplete/failure statuses and macOS-inferred claims, not just one UAT block. Require exactly one row for each of `pointer input`, `text/IME`, `eraser`, `click-through`, `style`, and `lifecycle`, platform Windows, green recorded status, exact command/job identity, exact five artifact references, Windows metadata, UTC timestamps, generated `overlay-display-*` evidence, and no skipped group. Keep validation/UAT incomplete until a real Windows run passes. Document the intentional deviation: a prior static overlay-label analog is insufficient because visible runtime surfaces are generated overlay-display-* and generated-window discovery is required by the diagnosed bug. Do not alter macOS evidence or infer Windows results from it.</action>
  <verify>
    <automated>test -x .planning/phases/03-core-annotation-tools/windows-evidence-gate.sh &amp;&amp; .planning/phases/03-core-annotation-tools/windows-evidence-gate.sh --check-scope</automated>
    <fails_when>The gate is absent/non-executable; it scans only Test 21; any Windows row, retained result/log/evidence file, generated-overlay evidence, or UAT gap/test/summary status is outside the scan; forbidden status/inference wording is not rejected; exact artifact names, all six groups, Windows identity, exact command/job, timestamps, or fail-closed skipped-group behavior is not enforced; or the command exits non-zero.</fails_when>
  </verify>
  <acceptance_criteria>The gate enumerates and scans every scoped Windows source and rejects incomplete/failing/skipped/macOS-inferred wording while requiring exact workflow, artifacts, host/timestamp, generated-overlay, and six-group contracts; pre-run validation/UAT remain truthful and incomplete.</acceptance_criteria>
  <done>The Windows evidence gate and evidence-consistent validation/UAT handoff are ready for blocking real-host execution; G-03-21 is not closed by this task.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 03-10-03: Run Windows phase3-tools and close G-03-21 from retained evidence</name>
  <read_first>
    - `.planning/phases/03-core-annotation-tools/03-PATTERNS.md` — host-separated evidence rules and generated-overlay discovery deviation.
    - `.github/workflows/phase3-native.yml` — exact phase3-tools job, commands, Windows guard, and artifact names.
    - `.planning/phases/03-core-annotation-tools/windows-evidence-gate.sh` — source-wide fail-closed scan.
    - `.planning/phases/03-core-annotation-tools/03-VALIDATION.md`, `.planning/phases/03-core-annotation-tools/03-UAT.md`, and `.planning/debug/g-03-21-windows-parity.md` — status fields, Test 21, and why macOS/cross-target output cannot qualify.
  </read_first>
  <precondition>A real Windows 2022 runner or Windows 10 22H2+ host is available; macOS execution and cross-target compilation are not eligible evidence.</precondition>
  <what-built>The exact Windows job/manual sequence and source-wide gate are present, with validation/UAT incomplete until the host run passes.</what-built>
  <how-to-verify>Run the exact Windows phase3-tools job for the current revision, inspect all five retained artifacts, confirm Windows host identity and UTC timestamps, confirm generated overlay-display-* targeting, confirm all six parity groups are green and not skipped, then run the source-wide gate before updating validation/UAT.</how-to-verify>
  <resume-signal>Approve only when the gate passes on the retained Windows bundle; otherwise report the missing artifact, forbidden status, inferred claim, host mismatch, generated-target failure, or parity group and keep G-03-21 open.</resume-signal>
  <action>Execute the configured phase3-tools job on a real Windows host and require the exact setup/build/native sequence and five artifact names. Confirm host-metadata.json identifies Windows OS/build, runner/device, WebView/runtime, and revision; timestamps.json has UTC start/end; the three logs/results retain command output, exit status, and native results. Confirm pointer input, text/IME, eraser, click-through, style, and lifecycle all pass with generated overlay-display-* targeting and no skipped group. Run the source-wide gate over every scoped Windows validation row, retained evidence file, generated-overlay block, and UAT gap/test/summary status. Only after it passes, update 03-VALIDATION.md and 03-UAT.md with separate Windows refs, Test 21 pass, G-03-21 closure, and truthful summary counts; preserve macOS rows and tray persistence semantics.</action>
  <verify>
    <automated>.planning/phases/03-core-annotation-tools/windows-evidence-gate.sh --verify-run .planning/phases/03-core-annotation-tools/windows-phase3-run.json</automated>
    <fails_when>The command exits non-zero; host is not Windows; any exact artifact is missing; timestamps/host identity/exit status are missing; any six-group result is absent, skipped, or failing; generated-overlay evidence is absent or names bootstrap only; any scoped source contains NOT RUN, PENDING, FAIL, SKIPPED, or macOS-inferred wording; or UAT/validation claims pass without this green Windows run.</fails_when>
  </verify>
  <acceptance_criteria>Retained evidence proves a green real Windows phase3-tools run with exact artifacts, exact command/job, Windows-only host identity, UTC timing, generated-overlay targeting, and six green parity groups; validation and Test 21 update only after the source-wide gate passes.</acceptance_criteria>
  <done>G-03-21 is closed from independently retained Windows evidence, with no macOS inference and with the Windows broken-evidence ledger eligible for its separate workflow update.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| GitHub workflow -> Windows runner | CI commands and artifact collection execute on an external host. |
| Windows runner -> retained evidence | Logs/results/metadata cross into repository-scoped evidence files. |
| Evidence files -> validation/UAT | Untrusted text becomes status claims only through the fail-closed gate and human review. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10-01 | Spoofing | Host/platform metadata | high | mitigate | Require Windows OS/build and runner/device identity, exact command/job identity, and reject non-Windows hosts. |
| T-03-10-02 | Tampering | Evidence bundle/status rows | high | mitigate | Require exact artifacts, timestamps, exit status, six unique groups, generated-overlay evidence, and source-wide forbidden-status scan. |
| T-03-10-03 | Repudiation | Workflow/native logs | medium | mitigate | Retain UTC timestamps, command output, result JSON, native log, revision, and host metadata on success/failure. |
| T-03-10-04 | Denial of Service | Skipped parity groups | high | mitigate | Fail closed when any required group is missing or skipped. |
| T-03-SC | Tampering | CI dependencies/artifact actions | low | accept | Reuse existing workflow conventions and locked dependencies; no package installation is planned. |
</threat_model>

<verification>
- Structural workflow check verifies exact Windows job/command/artifact/group/guard strings.
- Pre-run source-wide gate check proves every scoped source category is enumerated.
- Blocking host run passes the same gate against retained artifacts before validation/UAT status changes.
</verification>

<success_criteria>
- G-03-21: independent real Windows execution proves pointer input, text/IME, eraser, click-through, style, and lifecycle with exact retained artifacts and generated-overlay evidence.
- The evidence gate rejects incomplete, failed, skipped, or macOS-inferred claims across all scoped Windows sources and cannot be satisfied by a Test 21 keyword check.
</success_criteria>

<source_audit>
| Source | ID | Feature/decision | Plan | Status |
|--------|----|------------------|------|--------|
| GOAL | — | Presenter creates core annotations smoothly on supported platforms | 03-10 | COVERED |
| REQ | DRAW-01..DRAW-06 | Windows host evidence for every Phase 3 tool | 03-10 | COVERED |
| RESEARCH | Windows host/runtime evidence and platform separation | 03-10 | COVERED |
| CONTEXT | D-01..D-17 applicable platform/interaction rules | 03-10 | COVERED |
| CONTEXT | Deferred Ideas | undo/export/persistence/post-commit editing | excluded | EXCLUDED |
</source_audit>

<output>
Create `.planning/phases/03-core-annotation-tools/03-10-SUMMARY.md` when done.
</output>
