---
phase: 03-core-annotation-tools
plan: 10
type: execute
wave: 10
depends_on: [03-09]
files_modified: [.github/workflows/phase3-native.yml, .planning/phases/03-core-annotation-tools/windows-evidence-gate.sh, tests/contracts/phase3-workflow-contract.test.ts, tests/contracts/windows-evidence-gate.test.ts, tests/contracts/fixtures/windows-evidence-negative/]
autonomous: true
gap_closure: true
gap_ids: [G-03-21]
requirements: [DRAW-01, DRAW-02, DRAW-03, DRAW-04, DRAW-05, DRAW-06]
estimate: {tokens: 10000, raw_tokens: 10000, tasks: 2, confidence: high}
must_haves:
  truths:
    - "A Windows-only job named phase3-tools runs the exact frozen install, test, Rust test, build, debug build, and phase3-tools command sequence and retains the exact five evidence artifact names."
    - "The exact Windows evidence scope is: Windows validation rows, the retained Windows evidence directory and its files, windows-phase3-run.json, the generated-overlay evidence block, and UAT G-03-21/Test 21/summary/status fields; it explicitly excludes the non-Windows Test 22 section. Test 22 may remain SKIPPED only with its explicit non-Windows/manual reason; any SKIPPED in Windows rows/evidence or G-03-21/Test 21 scoped sources fails."
  artifacts: [.github/workflows/phase3-native.yml, .planning/phases/03-core-annotation-tools/windows-evidence-gate.sh, tests/contracts/windows-evidence-gate.test.ts]
  key_links: ["03-09 generated-overlay readiness -> Windows phase3-tools job -> exact evidence bundle -> scoped gate -> 03-11 status audit."]
---
<objective>Close the workflow and contract portion of G-03-21 without claiming a Windows runtime result.</objective>
<execution_context>@/Users/khacnghia/.codex/gsd-core/workflows/execute-plan.md
@/Users/khacnghia/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-annotation-tools/03-CONTEXT.md
@.planning/phases/03-core-annotation-tools/03-UAT.md
@.planning/phases/03-core-annotation-tools/03-VALIDATION.md
@.planning/debug/g-03-21-windows-parity.md
@.planning/phases/03-core-annotation-tools/03-09-PLAN.md</context>
<tasks>
<task type="tracer">
<name>Task 03-10-01: Add the exact Windows phase3-tools workflow</name>
<files>.github/workflows/phase3-native.yml, tests/contracts/phase3-workflow-contract.test.ts</files>
<read_first><item>.planning/phases/03-core-annotation-tools/03-09-PLAN.md — generated-overlay bridge contract.</item><item>.planning/debug/g-03-21-windows-parity.md — missing Windows evidence diagnosis.</item><item>.planning/phases/03-core-annotation-tools/03-VALIDATION.md — required Windows command sequence.</item></read_first>
<action>Define a Windows-only job with id and name exactly `phase3-tools`, a fail-closed Windows guard, commands in exact order `pnpm install --frozen-lockfile`, `pnpm test`, `cargo test --manifest-path src-tauri/Cargo.toml`, `pnpm build`, `pnpm exec tauri build --debug`, and `pnpm exec wdio run wdio.conf.ts --suite phase3-tools`. Require generated `overlay-display-*` evidence, six parity groups (`pointer input`, `text/IME`, `eraser`, `click-through`, `style`, `lifecycle`), Windows metadata/UTC timing, and exactly `host-metadata.json`, `timestamps.json`, `phase3-tools.log`, `phase3-tools-results.json`, and `native.log`, uploaded on failure. Keep macOS separate and never treat cross-compilation as Windows evidence.</action>
<verify><automated>pnpm exec vitest run tests/contracts/phase3-workflow-contract.test.ts</automated><fails_when>The command exits non-zero, or the exact job identity, command order, Windows guard, five artifact names, six groups, generated-overlay requirement, or upload-on-failure behavior is absent.</fails_when></verify>
<acceptance_criteria>The workflow contract proves the exact Windows job, commands, artifacts, guard, six groups, generated overlay, metadata, and failure upload.</acceptance_criteria>
<done>The Windows execution path is contractually defined; G-03-21 remains open for 03-11.</done>
</task>
<task type="auto">
<name>Task 03-10-02: Implement the fail-closed Windows evidence gate</name>
<files>.planning/phases/03-core-annotation-tools/windows-evidence-gate.sh, tests/contracts/windows-evidence-gate.test.ts, tests/contracts/fixtures/windows-evidence-negative/</files>
<read_first><item>.planning/phases/03-core-annotation-tools/03-UAT.md — Test 21, Test 22, and summary/status fields.</item><item>.planning/phases/03-core-annotation-tools/03-VALIDATION.md — Windows rows and evidence rules.</item><item>.planning/debug/g-03-21-windows-parity.md — prohibited macOS inference.</item></read_first>
<action>Implement the exact Windows evidence scope: Windows validation rows, the retained Windows evidence directory and its files, windows-phase3-run.json, the generated-overlay evidence block, and UAT G-03-21/Test 21/summary/status fields; explicitly exclude the non-Windows Test 22 section. Test 22 may remain SKIPPED only with its explicit non-Windows/manual reason; any SKIPPED in Windows rows/evidence or G-03-21/Test 21 scoped sources fails. Scan all scoped contents and reject `NOT RUN`, `PENDING`, `FAIL`, `SKIPPED`, macOS-inferred wording, missing/duplicate parity groups, missing exact artifacts/host/timestamps, nonzero exit, and cancellation. Add one negative fixture/test for each condition and a scope check. Require exact job/command, Windows host metadata, UTC timestamps, generated overlay, and all six groups. Do not alter implementation source or close G-03-21 before a real host run.</action>
<verify><automated>pnpm exec vitest run tests/contracts/windows-evidence-gate.test.ts</automated><fails_when>The command exits non-zero, the gate scans Test 22, fails to reject SKIPPED in any Windows/G-03-21/Test-21 scoped source, accepts any negative fixture, or omits a required source category/contract.</fails_when><automated>.planning/phases/03-core-annotation-tools/windows-evidence-gate.sh --check-scope</automated><fails_when>The command exits non-zero or does not report Windows validation/evidence/run/generated-overlay/UAT Test 21 sources while explicitly excluding non-Windows Test 22.</fails_when></verify>
<acceptance_criteria>The gate and fixtures use the exact Windows evidence scope, explicitly exclude Test 22, permit only Test 22's explicit non-Windows/manual SKIPPED reason, reject SKIPPED everywhere else in Windows/G-03-21/Test-21 scopes, and enforce the full artifact/host/timestamp/generated-overlay/six-group contract.</acceptance_criteria>
<done>The fail-closed gate is ready for the real Windows evidence and consistency checkpoint in 03-11.</done>
</task>
</tasks>
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| Workflow -> Windows runner | CI executes on an external host. |
| Evidence -> status | Retained text becomes claims only through a fail-closed gate. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-10-01 | Spoofing | Host identity | high | mitigate | Require Windows metadata and exact run identity. |
| T-03-10-02 | Tampering | Evidence scope | high | mitigate | Enforce allowlist, Test 22 exclusion, and forbidden statuses. |
| T-03-10-03 | Repudiation | Artifacts | medium | mitigate | Retain exact logs, results, and UTC timestamps. |
| T-03-SC | Tampering | CI actions | low | accept | Existing locked tooling is reused. |</threat_model>
<verification>Workflow and gate contract tests pass; scope check proves Test 22 exclusion.</verification>
<success_criteria>G-03-21 has a Windows-only executable evidence contract and cannot pass from macOS or configuration alone.</success_criteria>
<output>Create .planning/phases/03-core-annotation-tools/03-10-SUMMARY.md when done.</output>
