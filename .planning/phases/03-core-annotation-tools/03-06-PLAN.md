---
phase: 03-core-annotation-tools
plan: 06
type: execute
wave: 6
depends_on: [03-01, 03-02, 03-03, 03-04, 03-05]
files_modified:
  - src/components/OverlaySurface.tsx
  - src/components/overlay-surface.test.tsx
  - tests/e2e/core-annotation-tools.e2e.ts
autonomous: true
gap_closure: true
requirements: [DRAW-01, DRAW-02, DRAW-03, DRAW-04, DRAW-05, DRAW-06]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "On the macOS embedded-WebKit path, an explicit captured pointer sequence shows a realtime transient preview while held and commits exactly one retained scene item only after pointer-up."
    - "Pointer cancellation, Escape, focus loss, mode change, short geometry drags, and pointer-up outside the overlay leave the retained scene unchanged and do not produce duplicate commits."
    - "The Phase 3 smoke suite exercises the same pointer, keyboard, shape-fill, eraser, and click-through selectors on macOS and Windows, while its evidence identifies the host platform separately."
  artifacts:
    - "src/components/OverlaySurface.tsx exposes observable transient/gesture state and a capture-safe pointer termination path."
    - "src/components/overlay-surface.test.tsx covers the pointer termination decision and preserves transient-scene isolation."
    - "tests/e2e/core-annotation-tools.e2e.ts uses explicit W3C pointer actions and verifies preview-before-up and one-item-after-up behavior, including committed shape style."
  key_links:
    - "W3C pointerMove/down/move/up -> captured OverlaySurface pointer lifecycle -> transient marker -> onCommitSceneItem -> SceneSnapshot count/ID/style."
    - "Pointer cancellation/abort paths -> cancelGesture -> cleared transient state -> unchanged SceneStore snapshot."
    - "The same phase3-tools suite -> host-labeled macOS or Windows run -> validation evidence without cross-platform inference."
---

<objective>
Close the native input gap by diagnosing the macOS embedded-WebKit pointer-up failure, hardening the captured-pointer lifecycle, and making the smoke suite prove realtime preview, pointer-up commit, shape fill integration, and platform-separated evidence.

Purpose: The existing suite starts successfully but its drag helper does not establish the requested start-to-end captured gesture, leaving zero committed scene items and preventing native proof of all six drawing requirements.
Output: Capture-safe gesture instrumentation/regression coverage and a deterministic cross-platform Phase 3 smoke flow.
</objective>

<execution_context>
@/Users/khacnghia/.codex/gsd-core/workflows/execute-plan.md
@/Users/khacnghia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-core-annotation-tools/03-CONTEXT.md
@.planning/phases/03-core-annotation-tools/03-RESEARCH.md
@.planning/phases/03-core-annotation-tools/03-PATTERNS.md
@.planning/phases/03-core-annotation-tools/03-04-SUMMARY.md
@.planning/phases/03-core-annotation-tools/03-05-PLAN.md
@.planning/debug/stroke-not-realtime.md

<interfaces>
- `OverlaySurface` owns `setPointerCapture`, `pointerIdRef`, transient React state, `cancelGesture`, and the `onCommitSceneItem` callback; the native fix must preserve one shared canonical scene and existing mode gating.
- The current `dragCanvas` helper calls `dragAndDrop` without using its `start` argument; the replacement must send explicit viewport coordinates through one W3C pointer source.
- `sceneSnapshot()` currently exposes count and IDs; the integration assertion may call the existing `get_scene_snapshot` bridge to inspect the full committed style payload without adding a second scene store.
- `wdio.conf.ts` already registers `phase3-tools` against the embedded Tauri service and uses one worker; keep that cross-platform configuration unchanged unless a diagnostic-only change is required.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 03-06-01: Diagnose and harden the captured macOS pointer-up lifecycle</name>
  <read_first>
    - `.planning/phases/03-core-annotation-tools/03-PATTERNS.md` — exact-extension mappings for `src/components/OverlaySurface.tsx` and `src/components/overlay-surface.test.tsx`, plus the `tests/e2e/core-annotation-tools.e2e.ts` role analogs to `tests/e2e/overlay.e2e.ts` and `tests/e2e/display-topology.e2e.ts`.
    - `src/components/OverlaySurface.tsx` — `beginGesture`, `moveGesture`, `endGesture`, `cancelGesture`, pointer capture, and transient rendering.
    - `src/components/overlay-surface.test.tsx` — existing transient isolation, canonical pointer, and renderer call-order tests.
    - `tests/e2e/core-annotation-tools.e2e.ts` — current failing drag helper and retained-count assertion.
    - `wdio.conf.ts` — embedded macOS WebKit/Tauri service capabilities and timeouts.
    - `.planning/debug/stroke-not-realtime.md` — prior realtime-preview diagnosis and regression boundary.
    - `.planning/phases/03-core-annotation-tools/03-CONTEXT.md` — D-06, D-07, and D-10.
  </read_first>
  <behavior>
    - A captured pointerdown followed by pointermove exposes an observable transient gesture without increasing the retained scene count.
    - The matching pointerup finalizes the same gesture once even if WebKit retargets the event through pointer capture; mismatched pointer IDs and duplicate terminal events cannot commit.
    - Pointer cancel, lost capture, Escape, mode change, focus loss, and outside termination clear the transient candidate and leave retained scene data unchanged.
  </behavior>
  <files>src/components/OverlaySurface.tsx, src/components/overlay-surface.test.tsx</files>
  <action>
    Follow the `src/components/OverlaySurface.tsx` and `src/components/overlay-surface.test.tsx` exact-extension mappings in `03-PATTERNS.md`, preserving their canonical transform, transient rendering, pointer-capture, and call-order seams. Reproduce the failing lifecycle at the smallest boundary before changing behavior: expose scene-excluded diagnostic attributes on the canvas for `data-gesture-phase` (`idle`, `pressed`, `previewing`) and `data-transient-active`, then make the state transition explicit in `beginGesture`, `moveGesture`, `endGesture`, and `cancelGesture`. Per D-06, retain transient preview in React state and keep SceneSnapshot untouched until a valid terminal event; per D-07 and D-10, preserve threshold and abort semantics. Keep the active captured pointer identity and reject unrelated pointer IDs, but ensure the pointer-up branch finalizes the captured gesture exactly once when the same W3C pointer reaches the WebKit canvas path; clear/release before invoking `onCommitSceneItem`. Add an `onLostPointerCapture` cleanup that cancels only an unfinished gesture, and keep eraser/text branches outside the drawing capture path. Add focused renderer/lifecycle seam assertions for pressed-to-preview, terminal single-commit eligibility, duplicate/mismatched terminal events, and cancellation isolation. The diagnostic attributes are UI-only and must remain outside scene/export data.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/components/overlay-surface.test.tsx &amp;&amp; pnpm build</automated>
    <fails_when>non-zero exit, the targeted transient/pointer lifecycle assertions fail, a mismatched terminal event can commit, or the frontend build fails.</fails_when>
  </verify>
  <acceptance_criteria>Overlay lifecycle assertions show pressed-to-preview without scene mutation, one matching terminal commit, and cancellation/duplicate/mismatched/lost-capture isolation; diagnostics remain scene/export excluded.</acceptance_criteria>
  <done>The overlay has a capture-safe, observable pointer lifecycle that can distinguish realtime preview from retained scene state and finalizes one matching pointer-up without regressing canonical transforms or cancellation behavior.</done>
</task>

<task type="auto">
  <name>Task 03-06-02: Replace the WebKit drag helper and strengthen native Phase 3 integration evidence</name>
  <read_first>
    - `.planning/phases/03-core-annotation-tools/03-PATTERNS.md` — `tests/e2e/core-annotation-tools.e2e.ts` role analogs to `tests/e2e/overlay.e2e.ts` and `tests/e2e/display-topology.e2e.ts`, the `src/components/OverlaySurface.tsx` exact-extension mapping, and the `src/components/AnnotationToolbar.tsx` extraction mapping.
    - `tests/e2e/core-annotation-tools.e2e.ts` — current `dragCanvas`, pointer helpers, scene selectors, and tool flow.
    - `src/components/OverlaySurface.tsx` — diagnostic gesture/transient markers and canvas coordinates from Task 03-06-01.
    - `src/components/AnnotationToolbar.tsx` — shape fill-color/fill-opacity selectors from Plan 03-05.
    - `src/types/platform-parity.ts` — locked `TOOL_ORDER`.
    - `wdio.conf.ts` — existing `phase3-tools` registration; keep the embedded service and one-worker configuration.
    - `.planning/phases/03-core-annotation-tools/03-CONTEXT.md` — D-04, D-08, D-09, D-11, D-12, D-13, D-14, D-15, D-16, and D-17.
  </read_first>
  <behavior>
    - Each drawing gesture moves to the intended start coordinate, presses, moves while held, proves transient preview with an unchanged scene count, releases at the intended end coordinate, and observes exactly one new retained item.
    - Rectangle and ellipse controls are changed through the real property popover, then their committed snapshots contain distinct selected fillColor/fillOpacity values for the two active tools.
    - Text commit/cancel, eraser hover/click-only/topmost behavior, toolbar exclusion, and click-through mode remain covered after the pointer helper change.
  </behavior>
  <files>tests/e2e/core-annotation-tools.e2e.ts</files>
  <action>
    Follow the `tests/e2e/core-annotation-tools.e2e.ts` analog mapping in `03-PATTERNS.md` and retain the embedded-provider conventions from `tests/e2e/overlay.e2e.ts`, `tests/e2e/display-topology.e2e.ts`, and `wdio.conf.ts`; use the `src/components/OverlaySurface.tsx` diagnostics and `src/components/AnnotationToolbar.tsx` selectors only through their mapped contracts. Replace `dragCanvas`'s `dragAndDrop` call with explicit `browser.performActions` using one stable pointer source: move to the supplied start, issue primary pointerdown, move to the supplied end with a nonzero duration while the button remains held, assert `data-gesture-phase="previewing"` and `data-transient-active="true"` while `sceneSnapshot()` is unchanged, then issue pointerup at the same end and wait for one count/ID increase plus transient reset. Keep `start` and `end` as absolute viewport coordinates derived from `getBoundingClientRect`; release actions in cleanup and fail with the host/platform in diagnostics when the path stops at any phase. Add a `nativeSceneSnapshot` helper using the existing `get_scene_snapshot` invoke to inspect the last retained item. After selecting rectangle, drive `data-style-control="fillColor"` and `data-style-control="fillOpacity"` through WebDriver, wait for their controlled values, commit the shape, and assert its stored style matches; repeat with different ellipse values and assert rectangle and ellipse styles remain distinct. Preserve the existing ordered-tool, text keyboard, eraser, scene-exclusion, display identity, and click-through checks. Exercise the locked behavior from D-04, D-08, D-09, and D-11 through D-17 with the same selectors on both platforms; do not turn a macOS result into a Windows result, and do not add a test-only scene store or platform-specific tool vocabulary.
  </action>
  <verify>
    <automated>pnpm exec wdio run wdio.conf.ts --suite phase3-tools</automated>
    <fails_when>non-zero exit, the suite is missing, WebDriver cannot start, preview is absent before pointer-up, retained count does not increase by exactly one after pointer-up, a shape style assertion fails, or any existing text/eraser/click-through assertion fails.</fails_when>
  </verify>
  <acceptance_criteria>The native suite uses explicit W3C pointer actions, proves unchanged count during preview and exactly one new committed item after release, verifies distinct rectangle/ellipse styles, and retains text, eraser, scene-exclusion, and click-through checks.</acceptance_criteria>
  <done>The Phase 3 native suite uses a real start-to-end captured pointer sequence, proves preview-before-up and one-item-after-up, verifies shape fill control integration, and runs with the same host-labeled contract on macOS and Windows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebDriver pointer actions -> WebKit canvas | Native automation supplies untrusted coordinates and terminal events to the embedded webview. |
| Canvas gesture state -> Tauri scene command | Only a validated completed gesture may cross into the retained SceneStore. |
| Host execution -> validation evidence | macOS and Windows native behavior are separate evidence sources with different compositor/input implementations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06-01 | Tampering/Denial of Service | Captured pointer terminal events | high | mitigate | Keep a single active pointer identity, expose phase markers, clear before commit, reject mismatched/duplicate terminal events, and test abort paths. |
| T-03-06-02 | Tampering | Native smoke scene assertions | high | mitigate | Use the real UI and Tauri invoke bridge, assert unchanged count during preview, exact one-item count/ID change after release, and inspect the stored style payload. |
| T-03-06-03 | Tampering | Cross-platform evidence labeling | high | mitigate | Emit host/platform diagnostics and record macOS and Windows results in separate validation rows; a macOS run cannot satisfy the Windows row. |
| T-03-06-04 | Denial of Service | Click-through and scene-excluded chrome | high | mitigate | Preserve native click-through authority, explicit chrome markers, and the existing mode assertions after pointer helper changes. |
| T-03-SC | Tampering | Package-manager install surface | low | accept | No npm or crate installation is planned; use the locked WebdriverIO/Tauri dependencies already present. |
</threat_model>

<verification>
- Local gate: `pnpm exec vitest run src/components/overlay-surface.test.tsx && pnpm build`.
- Native gate: `pnpm exec wdio run wdio.conf.ts --suite phase3-tools` on each available host, with the result labeled by OS and runtime.
- Windows path: execute the same native command on a Windows 2022 runner or Windows 10 22H2+ device after the local test/build/debug-binary setup; record no Windows pass until that command and the manual Windows UAT in Plan 03-07 have dated evidence.
</verification>

<success_criteria>
- The macOS embedded-WebKit pointer sequence no longer leaves zero committed items when the real captured start-to-end action is used.
- Realtime preview, pointer-up single commit, cancellation, shape style integration, text lifecycle, eraser targeting, and click-through remain observable in the native suite.
- The exact suite is runnable on Windows and its result is kept separate from macOS evidence.
</success_criteria>

<output>
Create `.planning/phases/03-core-annotation-tools/03-06-SUMMARY.md` when done.
</output>
