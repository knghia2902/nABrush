---
phase: 03-core-annotation-tools
plan: 05
type: execute
wave: 5
depends_on: [03-02, 03-04]
files_modified:
  - src/components/AnnotationToolbar.tsx
  - src/components/annotation-toolbar.test.tsx
  - src/App.tsx
  - src/state/annotation.test.ts
autonomous: true
gap_closure: true
requirements: [DRAW-04]

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Presenter can open the compact property popover for rectangle or ellipse and set fill color and fill opacity through user-facing controls."
    - "Rectangle and ellipse keep independent fillColor and fillOpacity values when the presenter switches tools, and a committed shape keeps the values selected for its tool."
    - "The toolbar and property popover remain bottom-positioned, scene-excluded chrome in VisibleInteractive and preserve the existing click-through boundary."
  artifacts:
    - "src/components/AnnotationToolbar.tsx exposes controlled fill-color and fill-opacity inputs for the active shape tool."
    - "src/App.tsx routes the extracted toolbar through the existing stylesByTool update path without creating a second style store."
    - "src/components/annotation-toolbar.test.tsx proves the component renders the active shape controls and omits them for non-shape tools."
    - "src/state/annotation.test.ts proves fillColor and fillOpacity patches are immutable and independent per rectangle and ellipse tool."
  key_links:
    - "Active rectangle/ellipse tool -> AnnotationToolbar controlled inputs -> onUpdateStyle patch -> stylesByTool[activeTool] -> OverlaySurface gesture style snapshot."
    - "fillColor/fillOpacity control values -> shape style payload -> existing Canvas drawShapeGeometry fill path and native SceneStore snapshot."
    - "Toolbar extraction -> existing data-scene-excluded and TOOL_ORDER selectors used by the Phase 3 smoke suite."
---

<objective>
Close the DRAW-04 UI gap by making rectangle and ellipse fill color and fill opacity fully configurable through the existing per-tool property popover, with source and component assertions ready for native integration coverage.

Purpose: The renderer and native scene already retain fill values, but the user cannot reach two required style fields from the shipped toolbar.
Output: Extracted testable toolbar component, controlled shape fill controls, and immutable per-tool style/component tests.
</objective>

<execution_context>
@/Users/khacnghia/.codex/gsd-core/workflows/execute-plan.md
@/Users/khacnghia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-core-annotation-tools/03-CONTEXT.md
@.planning/phases/03-core-annotation-tools/03-RESEARCH.md
@.planning/phases/03-core-annotation-tools/03-PATTERNS.md
@.planning/phases/03-core-annotation-tools/03-02-SUMMARY.md
@.planning/phases/03-core-annotation-tools/03-04-SUMMARY.md

<interfaces>
- `AnnotationToolbar` is currently declared inside `src/App.tsx`; preserve its `AnnotationToolbarProps`, `TOOL_ORDER` button order, `aria-pressed`, `data-tool`, `data-property-popover`, and `data-scene-excluded` markers when extracting it.
- `updateToolStyle` already immutably updates one `stylesByTool` entry, and `AnnotationStyle` already contains `fill`, `fillColor`, and `fillOpacity`.
- `OverlaySurface` already consumes the active `toolStyle` and snapshots it into rectangle/ellipse scene items; this plan must keep that existing renderer/native contract intact.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 03-05-01: Trace active-shape fill controls through the existing toolbar state path</name>
  <read_first>
    - `.planning/phases/03-core-annotation-tools/03-PATTERNS.md` — `src/components/AnnotationToolbar.tsx` extraction mapping to the inline `src/App.tsx` toolbar and `src/styles.css` chrome layering, plus the exact `src/App.tsx` orchestrator analog.
    - `src/App.tsx` — current inline `AnnotationToolbar`, active-tool style selection, and update callback.
    - `src/state/annotation.ts` — `AnnotationStyle`, `stylesByTool`, `updateToolStyle`, and default shape styles.
    - `src/components/OverlaySurface.tsx` — shape gesture style snapshot and `drawShapeGeometry` consumers.
    - `src/styles.css` — bottom toolbar/property popover layout, scene exclusion, and pointer-event rules.
    - `.planning/phases/03-core-annotation-tools/03-CONTEXT.md` — D-01, D-03, D-04, D-05, and D-09.
  </read_first>
  <behavior>
    - The active rectangle or ellipse property popover shows a color input bound to `fillColor` and a 0–1 range input bound to `fillOpacity`.
    - Changing either control invokes the existing active-tool patch callback, and changing rectangle values does not mutate ellipse values or any other tool.
    - The extracted toolbar preserves bottom placement, compact popover behavior, canonical tool order, scene-excluded markers, and click-through-safe rendering.
  </behavior>
  <files>src/components/AnnotationToolbar.tsx, src/App.tsx</files>
  <action>
    Follow the `src/components/AnnotationToolbar.tsx` mapping in `03-PATTERNS.md`: extract the existing inline toolbar from the `src/App.tsx` analog and preserve the `src/styles.css` chrome-layering contract. Extract `AnnotationToolbar` from `src/App.tsx` into `src/components/AnnotationToolbar.tsx` with the same typed props and stable selectors, then import it from `App.tsx`; follow the `src/App.tsx` exact-extension mapping for the existing invoke/state callback boundary. Per D-01 and D-03, keep the toolbar at the bottom and expose properties only in the compact popover for the selected tool; per D-02, preserve one-click persistent tool selection. Per D-04 and D-09, keep all patches routed through `onUpdateStyle` for the active tool and add a `type="color"` control with `data-style-control="fillColor"` bound to `toolStyle.fillColor`, plus a `type="range"` control with `data-style-control="fillOpacity"`, `min="0"`, `max="1"`, and `step="0.01"` bound to `toolStyle.fillOpacity`, inside the existing rectangle/ellipse branch. Keep the existing fill-mode selector, color, opacity, width, and text-size behavior unchanged. Preserve D-05's thinner defaults by leaving defaults in `src/state/annotation.ts`, and preserve all scene-excluded and pointer-event markers so the toolbar remains UI chrome rather than a scene item.
  </action>
  <verify>
    <automated>pnpm build</automated>
    <fails_when>non-zero exit, TypeScript cannot resolve the extracted component or its props, or the Vite production build fails.</fails_when>
  </verify>
  <acceptance_criteria>The extracted toolbar is imported by `App.tsx`; rectangle and ellipse expose controlled `fillColor` and `fillOpacity` inputs with the required selectors and range; other tools retain their existing controls and the toolbar remains scene-excluded.</acceptance_criteria>
  <done>The active rectangle and ellipse popovers expose controlled fill-color and fill-opacity inputs through the existing per-tool style update path, and the frontend build remains green.</done>
</task>

<task type="auto">
  <name>Task 03-05-02: Add source and component assertions for independent shape fill state</name>
  <read_first>
    - `.planning/phases/03-core-annotation-tools/03-PATTERNS.md` — `src/components/annotation-toolbar.test.tsx` role-match to `src/components/mode-badge.test.tsx` and `src/state/annotation.test.ts` role-match to `src/state/overlay.test.ts`.
    - `src/components/AnnotationToolbar.tsx` — extracted controlled toolbar component and shape-only branches from Task 03-05-01.
    - `src/state/annotation.ts` — immutable `updateToolStyle` contract and the eight-tool style map.
    - `src/state/annotation.test.ts` — existing Vitest fixture/style assertions.
    - `src/components/mode-badge.test.tsx` — `renderToStaticMarkup` component-test convention without adding a UI testing dependency.
    - `.planning/phases/03-core-annotation-tools/03-CONTEXT.md` — D-04 and D-09.
  </read_first>
  <behavior>
    - Static component markup for rectangle and ellipse contains `fillColor`, `fillOpacity`, and fill-mode controls with the supplied values; static markup for pen omits shape-only controls.
    - Updating rectangle fill color/opacity returns a new state and leaves ellipse, pen, and the original rectangle state unchanged; updating ellipse afterward preserves rectangle values.
    - The assertions cover the exact controlled input names and ranges that the native integration test will drive.
  </behavior>
  <files>src/components/annotation-toolbar.test.tsx, src/state/annotation.test.ts</files>
  <action>
    Follow the `src/components/annotation-toolbar.test.tsx` mapping in `03-PATTERNS.md` by using the `src/components/mode-badge.test.tsx` `react-dom/server` static-markup convention, and follow the `src/state/annotation.test.ts` mapping to `src/state/overlay.test.ts` for pure immutable state assertions. Add `src/components/annotation-toolbar.test.tsx` using that static-markup convention to render the extracted component with rectangle, ellipse, and pen fixtures. Assert the shape markup includes `data-style-control="fillColor"` and `data-style-control="fillOpacity"`, the supplied controlled values, the 0–1 range, and the scene-excluded popover marker; assert non-shape markup omits shape-only controls while retaining the shared controls. Extend `src/state/annotation.test.ts` with source-level behavior assertions using `createInitialAnnotationState` and `updateToolStyle`: per D-04 and D-09, patch rectangle `fillColor`/`fillOpacity`, switch to ellipse and patch different values, then prove every style object is independent and the original state is unchanged. Do not add a testing package or a second style state implementation; leave native integration of the actual committed shape payload to the dependent pointer/smoke plan.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/components/annotation-toolbar.test.tsx src/state/annotation.test.ts &amp;&amp; pnpm build</automated>
    <fails_when>non-zero exit, a component marker/value/range assertion fails, per-tool immutability is violated, or the production build fails.</fails_when>
  </verify>
  <acceptance_criteria>Component assertions cover rectangle, ellipse, and pen markup; state assertions prove independent immutable rectangle/ellipse fill patches; the targeted test command and build both pass.</acceptance_criteria>
  <done>Source/state and component assertions prove the two missing shape controls are active-tool controlled and preserve independent rectangle/ellipse values without adding dependencies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Toolbar DOM input -> React style state | User-provided color and opacity values enter the active-tool style map. |
| React style state -> retained SceneItem | The selected style crosses into a gesture snapshot and then the existing Tauri commit boundary. |
| Scene-excluded chrome -> native overlay hit testing | Toolbar and popover must remain usable only in drawing mode and must not intercept click-through input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05-01 | Tampering | `fillColor`/`fillOpacity` controls | medium | mitigate | Use controlled inputs, active-tool-scoped `updateToolStyle`, bounded HTML ranges, and preserve Rust validation already provided by Plans 03-01/03-02. |
| T-03-05-02 | Tampering | Per-tool style map | medium | mitigate | Add immutable rectangle/ellipse isolation tests and snapshot style only through the existing `OverlaySurface` gesture path. |
| T-03-05-03 | Denial of Service | Toolbar/property popover layering | high | mitigate | Preserve D-01/D-03 scene-excluded markers, explicit interactive descendants, and the native click-through authority. |
| T-03-SC | Tampering | Package-manager install surface | low | accept | No npm or crate installation is planned; use the locked dependencies already present. |
</threat_model>

<verification>
- Automated local gate: `pnpm exec vitest run src/components/annotation-toolbar.test.tsx src/state/annotation.test.ts && pnpm build`.
- Dependent native gate: the `phase3-tools` suite must drive these controls and inspect the committed rectangle/ellipse style snapshot after the pointer-up path is repaired.
</verification>

<success_criteria>
- Rectangle and ellipse users can set fill color and fill opacity from the active-tool property popover.
- Source/state and component tests prove the controls are controlled, shape-scoped, and independent per tool.
- Existing toolbar order, bottom placement, scene exclusion, click-through behavior, and native validation seams remain intact.
</success_criteria>

<output>
Create `.planning/phases/03-core-annotation-tools/03-05-SUMMARY.md` when done.
</output>
