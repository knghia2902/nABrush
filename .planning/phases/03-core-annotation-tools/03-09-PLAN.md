---
phase: 03-core-annotation-tools
plan: 09
type: execute
wave: 9
depends_on: [03-06, 03-08]
files_modified: [package.json, pnpm-lock.yaml, src/main.tsx, src-tauri/tauri.conf.json, src-tauri/src/main.rs, src-tauri/capabilities/default.json, wdio.conf.ts, tests/e2e/overlay.e2e.ts, tests/e2e/core-annotation-tools.e2e.ts]
autonomous: false
gap_closure: true
gap_ids: [G-03-18, G-03-20]
requirements: [DRAW-01, DRAW-02, DRAW-03, DRAW-04, DRAW-05, DRAW-06]
estimate: {tokens: 13000, raw_tokens: 13000, tasks: 3, confidence: high}
must_haves:
  truths:
    - "On macOS, the exact @wdio/tauri-plugin bridge is initialized and awaited before native commands, window enumeration, or target switching."
    - "After Show, native smoke targets a visible generated overlay-display-* window, verifies its canvas and lifecycle state, and never treats the hidden bootstrap overlay as the drawing surface."
    - "The generated macOS target accepts real pointer and keyboard input: preview appears before pointer-up, a valid gesture commits exactly one retained item, and text draft Enter/Shift+Enter/Esc behavior is observable without scene pollution."
    - "The tray-owned process may remain alive after a runner or window close; process exit is required only after an explicit Quit assertion."
  artifacts: ["wdio.conf.ts and src/main.tsx expose awaited bridge readiness and generated-overlay discovery diagnostics.", "tests/e2e/overlay.e2e.ts and tests/e2e/core-annotation-tools.e2e.ts exercise the generated overlay-display-* target for lifecycle, pointer, and text input."]
  key_links: ["Guest bridge readiness -> Show/reconcile -> visible overlay-display-* handle -> W3C pointer/keyboard input -> SceneSnapshot assertions.", "Generated-window discovery is shared by phase3-tools, short-lifecycle, and phase1-matrix; static hidden overlay remains bootstrap-only."]
---

<objective>Close G-03-18 and G-03-20 on macOS by making the supported Tauri WebDriver bridge ready before use and driving native assertions against the visible generated overlay window.
Purpose: The diagnosed failure is at the bridge/window/input boundary, not in the already-tested retained scene implementation.
Output: A deterministic macOS bridge/readiness helper and passing native lifecycle, pointer, and text draft flows on the generated overlay target.</objective>

<execution_context>
@/Users/khacnghia/.codex/gsd-core/workflows/execute-plan.md
@/Users/khacnghia/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-annotation-tools/03-CONTEXT.md
@.planning/phases/03-core-annotation-tools/03-RESEARCH.md
@.planning/phases/03-core-annotation-tools/03-PATTERNS.md
@.planning/phases/03-core-annotation-tools/03-UAT.md
@.planning/phases/03-core-annotation-tools/03-VERIFICATION.md
@.planning/phases/03-core-annotation-tools/03-VALIDATION.md
@.planning/debug/text-tool-draft-g-03-18.md
@.planning/debug/g-03-20-native-lifecycle.md
</context>
<tasks>
<task type="checkpoint:human-verify" gate="blocking-human">
<name>Task 03-09-00: Verify the exact WDIO bridge package</name>
<what-built>The exact bridge package identity and paired service requirements are available for approval.</what-built>
<how-to-verify>Run the identity check and inspect the package metadata, then approve only the exact package.</how-to-verify>
<resume-signal>Approve the exact package or leave the bridge task blocked.</resume-signal>
<read_first><item>`node_modules/@wdio/tauri-service/package.json` — installed service metadata.</item><item>`node_modules/@wdio/tauri-service/docs/plugin-setup.md` — exact bridge import and paired Rust requirements.</item><item>`.planning/phases/03-core-annotation-tools/03-RESEARCH.md` — package legitimacy policy.</item></read_first>
<action>Confirm that the registry package named exactly `@wdio/tauri-plugin` is WebdriverIO-maintained and paired with `@wdio/tauri-service@1.4.0`; verify identity before adding it and approve installation only for G-03-20. Do not substitute another package or generic bridge.</action>
<verify><automated>node /Users/khacnghia/.codex/gsd-core/bin/gsd-tools.cjs query package-legitimacy check --ecosystem npm @wdio/tauri-plugin</automated><fails_when>The command exits non-zero, the package does not resolve as `@wdio/tauri-plugin`, its identity is not WebdriverIO-maintained, or blocking human approval is absent.</fails_when></verify>
<acceptance_criteria>The exact `@wdio/tauri-plugin` package is identity-verified and explicitly approved before manifest changes.</acceptance_criteria>
<done>Package legitimacy approval is recorded and only the exact documented bridge package may be installed.</done>
</task>
<task type="tracer">
<name>Task 03-09-01: Wire macOS bridge readiness to the visible generated overlay</name>
<files>package.json, pnpm-lock.yaml, src/main.tsx, src-tauri/tauri.conf.json, src-tauri/src/main.rs, src-tauri/capabilities/default.json, wdio.conf.ts, tests/e2e/overlay.e2e.ts</files>
<read_first><item>`.planning/phases/03-core-annotation-tools/03-PATTERNS.md` — native/window evidence and generated-window rules.</item><item>`.planning/phases/03-core-annotation-tools/03-CONTEXT.md` — D-01, D-02, D-03, D-06, D-10, D-11, D-12, D-13.</item><item>`.planning/debug/g-03-20-native-lifecycle.md` — bridge, hidden bootstrap, generated labels, tray persistence.</item><item>`wdio.conf.ts`, `src/main.tsx`, `src-tauri/tauri.conf.json`, `src-tauri/capabilities/default.json`, `tests/e2e/overlay.e2e.ts` — current setup.</item></read_first>
<action>Add `@wdio/tauri-plugin` to the manifests, add literal side-effect import `import "@wdio/tauri-plugin"` in `src/main.tsx`, register `tauri_plugin_wdio::init()` in `src-tauri/src/main.rs` while retaining the existing embedded `tauri-plugin-wdio-webdriver` plugin, set `app.withGlobalTauri` to `true`, and grant `wdio:default`. Poll `window.__wdio_original_core__` and fail before commands, enumeration, or switching when absent. Wait for bootstrap, invoke Show, enumerate labels, select only a visible generated `overlay-display-*` window, switch to it, and recheck canvas/lifecycle readiness. Apply this to short-lifecycle and phase1-matrix while retaining static `overlay` only as bootstrap state. Preserve normal command bridging and tray persistence; require exit only after explicit Quit. This deviates from the static-label analog because visible runtime surfaces are generated `overlay-display-*` windows.</action>
<verify><automated>pnpm build &amp;&amp; pnpm exec wdio run wdio.conf.ts --suite short-lifecycle &amp;&amp; pnpm exec wdio run wdio.conf.ts --suite phase1-matrix</automated><fails_when>Any command exits non-zero; the exact bridge import, `tauri_plugin_wdio::init()`, `wdio:default`, or `withGlobalTauri: true` is absent; `window.__wdio_original_core__` is not awaited before commands/enumeration/switching; diagnostics omit host/bridge/labels/selected label; Show has no visible generated target; hidden `overlay` is selected; canvas/lifecycle readiness is absent; or lifecycle incorrectly requires tray-process exit without explicit Quit.</fails_when></verify>
<acceptance_criteria>The macOS runner uses the exact bridge import with `tauri_plugin_wdio::init()`, `wdio:default`, and `withGlobalTauri: true`; awaits `window.__wdio_original_core__`; discovers and switches to a visible generated `overlay-display-*` window after Show; and applies the contract to both lifecycle suites.</acceptance_criteria>
<done>G-03-20 bridge, target identity, readiness, and lifecycle setup conditions are satisfied without changing scene behavior.</done>
</task>
<task type="auto">
<name>Task 03-09-02: Prove macOS pointer and text draft behavior on the generated target</name>
<files>wdio.conf.ts, tests/e2e/overlay.e2e.ts, tests/e2e/core-annotation-tools.e2e.ts</files>
<read_first><item>`.planning/phases/03-core-annotation-tools/03-PATTERNS.md` — transient preview, text draft, hit-test, and evidence patterns.</item><item>`.planning/phases/03-core-annotation-tools/03-UAT.md` — Test 18 and Test 20 outcomes.</item><item>`.planning/debug/text-tool-draft-g-03-18.md` and `.planning/debug/g-03-20-native-lifecycle.md` — diagnosed input boundary.</item><item>`wdio.conf.ts`, `tests/e2e/overlay.e2e.ts`, `tests/e2e/core-annotation-tools.e2e.ts` — existing assertions.</item></read_first>
<action>Use the readiness helper's visible generated target for every W3C pointer and keyboard action. Assert preview before pointer-up, exactly one retained commit, text draft visibility/focus, non-composing Enter commit, Shift+Enter newline, Esc cancellation with unchanged scene count, and existing tool/style/fill/eraser/click-through/cancellation behavior. Keep editor and diagnostics scene-excluded, record host/generated-window identity, and honor D-06, D-10, D-11, D-12, D-13, D-14, D-15, D-16, and D-17.</action>
<verify><automated>pnpm exec vitest run src/components/overlay-surface.test.tsx &amp;&amp; pnpm exec wdio run wdio.conf.ts --suite phase3-tools</automated><fails_when>Any command exits non-zero; selected label is not a visible generated `overlay-display-*` window; preview is absent before pointer-up; valid gesture does not commit exactly one item; text draft/focus, Enter, Shift+Enter, or Esc fails; existing assertions regress; or retained scene data includes transient/editor state.</fails_when></verify>
<acceptance_criteria>macOS phase3-tools reaches the real generated overlay and passes native pointer preview/commit plus text draft Enter/Shift+Enter/Esc lifecycle while preserving Phase 3 scene semantics.</acceptance_criteria>
<done>G-03-18 is closed and macOS G-03-20 native behavior is evidenced; no Windows status changes.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| WebDriver host -> Tauri guest | Native input and commands cross into the webview. |
| Bootstrap -> generated overlay | Lifecycle and window identity cross into runtime surfaces. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-09-01 | Tampering | WDIO bridge | high | mitigate | Await bridge readiness and fail before assertions. |
| T-03-09-02 | Spoofing | Window discovery | high | mitigate | Select only visible generated targets and verify after switching. |
| T-03-09-03 | Tampering | Pointer/text evidence | high | mitigate | Assert preview, exact-one commit, keyboard semantics, and scene isolation. |
| T-03-09-04 | Denial of Service | Tray lifecycle | medium | mitigate | Treat close persistence as expected; exit only after explicit Quit. |
| T-03-SC | Tampering | WDIO package | high | mitigate | Legitimacy checkpoint verifies the exact package. |
</threat_model>
<verification>Run `pnpm build`, focused renderer tests, short-lifecycle, phase1-matrix, and phase3-tools on macOS; all suites must use awaited bridge readiness and generated-overlay discovery.</verification>
<success_criteria>G-03-18 text lifecycle passes on the real generated overlay; G-03-20 bridge readiness and generated target identity pass without hidden-bootstrap assertions.</success_criteria>
<source_audit>GOAL, DRAW-01..DRAW-06, R-09, and D-01..D-17 are covered by 03-09; deferred ideas remain excluded.</source_audit>
<output>Create `.planning/phases/03-core-annotation-tools/03-09-SUMMARY.md` when done.</output>
