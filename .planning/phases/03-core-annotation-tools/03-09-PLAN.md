---
phase: 03-core-annotation-tools
plan: 09
type: execute
wave: 9
depends_on: [03-06, 03-08]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/main.tsx
  - src-tauri/tauri.conf.json
  - src-tauri/capabilities/default.json
  - wdio.conf.ts
  - tests/e2e/overlay.e2e.ts
  - tests/e2e/core-annotation-tools.e2e.ts
  - .github/workflows/phase1.yml
  - .github/workflows/phase3-native.yml
  - .planning/phases/03-core-annotation-tools/03-VALIDATION.md
  - .planning/phases/03-core-annotation-tools/windows-phase3-run.json
  - .planning/phases/03-core-annotation-tools/windows-phase3-tools-evidence/
autonomous: false
gap_closure: true
gap_ids: [G-03-18, G-03-20, G-03-21]
requirements: [DRAW-01, DRAW-02, DRAW-03, DRAW-04, DRAW-05, DRAW-06]

estimate:
  tokens: 22000
  raw_tokens: 18000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "On macOS, the supported WDIO Tauri guest bridge is ready before native commands or window selection, Show discovers a visible overlay-display-* target, and phase3-tools proves native pointer delivery, realtime preview, exactly-one commit, and the complete text draft keyboard lifecycle."
    - "The configured smoke target is the visible generated per-display overlay rather than the hidden bootstrap overlay, while tray-owned process persistence remains expected after the runner exits until Quit is invoked."
    - "Windows parity is backed by a real Windows host/runner execution of the full phase3-tools matrix with timestamped logs/results and explicit host-level checks; no macOS result is copied into the Windows row."
  artifacts:
    - "wdio.conf.ts and the WDIO/Tauri guest setup provide an awaited, observable bridge-readiness and visible-overlay discovery contract."
    - "tests/e2e/core-annotation-tools.e2e.ts proves pointer preview/commit and text draft visibility, Enter commit, Shift+Enter newline, and Esc cancel on the discovered generated overlay."
    - ".github/workflows/phase3-native.yml and the Windows execution path retain timestamped phase3-tools logs/results and host metadata for review, and a green Windows run is attached before G-03-21 is closed."
    - ".planning/phases/03-core-annotation-tools/03-VALIDATION.md records separate macOS and Windows evidence for pointer, text/IME, eraser, click-through, style, and lifecycle behavior."
  key_links:
    - "WDIO guest bridge readiness -> Show/reconcile -> visible overlay-display-* window handle -> W3C pointer/keyboard input -> retained SceneSnapshot evidence."
    - "Generated window discovery -> phase3-tools, short-lifecycle, and phase1-matrix target selection without falling back to hidden overlay."
    - "Windows host workflow/manual matrix -> timestamped artifact upload -> Windows-only validation row; macOS output cannot satisfy it."

---

<objective>
Close the remaining Phase 3 native evidence gaps by making the Tauri WebDriver bridge and generated overlay target deterministic on macOS, then providing an executable Windows parity path with retained host evidence.

Purpose: The implementation and unit suites already cover the annotation model, but native proof is blocked by an incomplete WDIO guest bridge, selection of the hidden bootstrap window, and the absence of any independent Windows runtime result.
Output: One macOS-native flow that reaches the visible overlay and proves pointer/text behavior, plus a real Windows phase3-tools evidence path and evidence-consistent validation updates.
</objective>

<execution_context>
@/Users/khacnghia/.codex/gsd-core/workflows/execute-plan.md
@/Users/khacnghia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-annotation-tools/03-UAT.md
@.planning/phases/03-core-annotation-tools/03-VERIFICATION.md
@.planning/phases/03-core-annotation-tools/03-VALIDATION.md
@.planning/phases/03-core-annotation-tools/03-CONTEXT.md
@.planning/phases/03-core-annotation-tools/03-06-PLAN.md
@.planning/phases/03-core-annotation-tools/03-08-PLAN.md
@.planning/debug/text-tool-draft-g-03-18.md
@.planning/debug/g-03-20-native-lifecycle.md
@.planning/debug/g-03-21-windows-parity.md
</context>

<tasks>

<task type="tracer">
  <name>Task 03-09-01: Make the macOS bridge and generated overlay target native-ready</name>
  <read_first>
    - `.planning/phases/03-core-annotation-tools/03-UAT.md` — G-03-18 and G-03-20 missing evidence and the required native text/lifecycle outcomes.
    - `.planning/phases/03-core-annotation-tools/03-VERIFICATION.md` — native pointer-flow gap and the boundary between source proof and host-observed proof.
    - `.planning/phases/03-core-annotation-tools/03-VALIDATION.md` — exact existing phase3-tools, short-lifecycle, phase1-matrix, and platform evidence contract.
    - `.planning/phases/03-core-annotation-tools/03-CONTEXT.md` — D-06, D-10, D-11, D-12, D-13, D-14, D-15, D-16, and D-17 behavior constraints.
    - `.planning/phases/03-core-annotation-tools/03-06-PLAN.md` — existing pointer-preview/one-commit contract and selectors; extend it rather than creating a second scene path.
    - `.planning/debug/text-tool-draft-g-03-18.md` — evidence that text draft/commit works once placement pointerdown reaches the handler.
    - `.planning/debug/g-03-20-native-lifecycle.md` — WDIO guest bridge requirements, hidden bootstrap versus generated overlay labels, and tray persistence diagnosis.
    - `wdio.conf.ts` — current embedded Tauri service, suite registration, and host diagnostics.
    - `tests/e2e/overlay.e2e.ts` — existing lifecycle/window-switching helpers and direct application invoke conventions.
    - `tests/e2e/core-annotation-tools.e2e.ts` — current phase3-tools pointer, text, eraser, click-through, and scene assertions.
    - `src/main.tsx` — frontend entrypoint where the supported WDIO Tauri guest bridge must be initialized.
    - `src-tauri/tauri.conf.json` — current `withGlobalTauri` and window configuration.
    - `src-tauri/capabilities/default.json` — current capability/window permission boundary.
  </read_first>
  <behavior>
    - The supported WDIO Tauri guest bridge is present and awaited before `browser.tauri.execute`, window enumeration, or application invoke; readiness failure includes the host, bridge state, and current window labels.
    - After Show, the runner enumerates windows, selects a visible `overlay-display-*` label, verifies its document/canvas readiness and current lifecycle mode, and never treats the hidden bootstrap `overlay` label as the visible drawing surface.
    - On that generated target, a real W3C pointer sequence shows transient preview before pointer-up and commits exactly one retained item after pointer-up; native text placement visibly creates the draft, `Enter` commits, `Shift+Enter` inserts a newline, and `Esc` cancels without changing retained scene data.
    - Closing the window or ending a runner leaves the tray-owned process alive as expected; only an explicit Quit assertion may require process exit.
  </behavior>
  <files>package.json, pnpm-lock.yaml, src/main.tsx, src-tauri/tauri.conf.json, src-tauri/capabilities/default.json, wdio.conf.ts, tests/e2e/overlay.e2e.ts, tests/e2e/core-annotation-tools.e2e.ts</files>
  <action>
    Configure the supported WDIO Tauri guest bridge according to the diagnosed contract in G-03-20, including the guest-side plugin/bootstrap, Tauri global bridge setting, and narrowly scoped capability permission needed by the existing embedded service; use the repository's locked dependency path and preserve the app's normal command bridge. Add one readiness helper that waits for the guest bridge and application bootstrap, calls Show, enumerates window labels, filters for visible generated `overlay-display-*` windows, switches to the selected generated target, and re-checks lifecycle/canvas readiness after switching. Remove hard-coded assumptions that the static `overlay` bootstrap is the visible canvas while retaining it only as an allowed discovery/bootstrap handle. Extend the existing phase3-tools flow to use the discovered target for W3C pointer actions and keyboard input, assert preview-before-up and one retained commit, then assert text draft visibility at the click point, non-composing Enter commit, Shift+Enter newline in the draft, and Esc cancellation with an unchanged scene count. Apply the same target/readiness contract to short-lifecycle and phase1-matrix helpers. Keep all tool, style, eraser, click-through, and display assertions already covered by Plan 03-06. Record host/window labels and bridge readiness in diagnostics, and explicitly classify tray process persistence as expected unless the test invokes Quit. Honor D-06/D-10 for transient and cancel behavior, D-11/D-12/D-13 for text lifecycle, and D-14/D-15/D-16/D-17 for eraser semantics; this task changes only bridge/configuration/test wiring and evidence, not the retained scene model or annotation behavior.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/components/overlay-surface.test.tsx &amp;&amp; pnpm build &amp;&amp; pnpm exec wdio run wdio.conf.ts --suite short-lifecycle &amp;&amp; pnpm exec wdio run wdio.conf.ts --suite phase3-tools &amp;&amp; pnpm exec wdio run wdio.conf.ts --suite phase1-matrix</automated>
    <fails_when>any command exits non-zero; the guest bridge is not ready before native calls; Show yields no visible `overlay-display-*` target; the runner selects hidden `overlay`; preview is absent before pointer-up; pointer-up does not add exactly one item; text draft is not visible; Enter, Shift+Enter, or Esc behavior is wrong; lifecycle/style/eraser/click-through assertions regress; or the evidence incorrectly requires tray-process exit without an explicit Quit.</fails_when>
  </verify>
  <acceptance_criteria>The macOS suites target the visible generated overlay, bridge/readiness diagnostics are deterministic, native pointer preview and single commit pass, and text draft visibility/Enter/Shift+Enter/Esc behavior passes on the real overlay without changing retained scene semantics.</acceptance_criteria>
  <done>G-03-18 and the macOS portion of G-03-20 are closed by a passing native path that reaches the generated overlay-display-* window and proves the required pointer and text lifecycle; tray persistence is documented as expected until Quit.</done>
</task>

<task type="auto">
  <name>Task 03-09-02: Configure independent Windows parity execution and timestamped evidence</name>
  <read_first>
    - `.planning/phases/03-core-annotation-tools/03-UAT.md` — G-03-21 missing Windows artifacts and the six required host-level behavior groups.
    - `.planning/phases/03-core-annotation-tools/03-VERIFICATION.md` — Windows cannot be inferred from macOS and native evidence remains incomplete until host execution exists.
    - `.planning/phases/03-core-annotation-tools/03-VALIDATION.md` — the runnable Windows setup sequence, manual evidence matrix, and current NOT RUN boundary.
    - `.planning/phases/03-core-annotation-tools/03-CONTEXT.md` — D-01 through D-17 scope/interaction constraints and the Phase 3 fence.
    - `.planning/phases/03-core-annotation-tools/03-06-PLAN.md` — cross-platform phase3-tools contract and platform-separated assertions.
    - `.planning/phases/03-core-annotation-tools/03-08-PLAN.md` — evidence-consistent validation/status rules.
    - `.planning/debug/g-03-21-windows-parity.md` — confirmed evidence gap, existing workflow omission, and Windows host/toolchain boundary.
    - `.github/workflows/phase1.yml` — current Windows job conventions and artifact handling to extend without weakening existing jobs.
    - `wdio.conf.ts` — suite selection, host labeling, and generated-overlay readiness contract from Task 03-09-01.
    - `tests/e2e/core-annotation-tools.e2e.ts` — full phase3-tools matrix whose Windows run must be real and independently reported.
  </read_first>
  <behavior>
    - A Windows 2022 runner or Windows 10 22H2+ manual host executes the complete setup/build/native sequence and `pnpm exec wdio run wdio.conf.ts --suite phase3-tools` on Windows, not a macOS cross-target substitute.
    - The Windows record includes timestamp, OS/build identity, runner/device identity, command exit status, and retained logs/results for pointer, text/IME, eraser, click-through, style, lifecycle, and generated-overlay targeting.
    - Windows evidence remains a separate validation row; missing Windows execution remains NOT RUN rather than being marked PASS from macOS output, and tray persistence remains expected unless Quit is invoked.
  </behavior>
  <files>.github/workflows/phase1.yml, .github/workflows/phase3-native.yml, wdio.conf.ts, tests/e2e/core-annotation-tools.e2e.ts, .planning/phases/03-core-annotation-tools/03-VALIDATION.md</files>
  <action>
    Add the Windows workflow and manual-run configuration for the full Phase 3 tools matrix, preferably as a dedicated Windows CI job/workflow using the repository's existing setup sequence: frozen dependency install, frontend tests, Rust tests, production build, debug Tauri build, then `pnpm exec wdio run wdio.conf.ts --suite phase3-tools`. Ensure the job runs after Task 03-09-01's generated-overlay/bridge contract is available, emits UTC timestamps and host/OS/WebView/build metadata, captures WebDriver/native logs, command output, screenshots or result JSON where the existing suite exposes them, and uploads them as retained CI artifacts even on failure. Keep macOS and Windows jobs separate and make the runner fail if the host is not Windows or if any required matrix group is skipped. Add the companion manual execution checklist for pointer preview/commit, text/IME with Enter/Shift+Enter/Esc, eraser hover/topmost/click-only behavior, click-through, per-tool style/fill, lifecycle/show-hide/emergency recovery, and each connected display. Preserve the rule that Windows is PASS only from Windows host evidence, never from macOS, and retain explicit NOT RUN/PENDING status if the runner or manual host is unavailable until the separate execution gate in Task 03-09-03 is satisfied. Keep tray process persistence explicit as expected after window close/runner completion unless Quit is invoked. Do not expand the phase into deferred undo, persistence, export, or post-commit text editing.
  </action>
  <verify>
    <automated>test -f .github/workflows/phase3-native.yml &amp;&amp; rg -q 'windows|Windows' .github/workflows/phase3-native.yml &amp;&amp; rg -q 'phase3-tools' .github/workflows/phase3-native.yml &amp;&amp; rg -q 'upload-artifact|artifact' .github/workflows/phase3-native.yml &amp;&amp; rg -q 'timestamp|UTC|os|runner' .github/workflows/phase3-native.yml &amp;&amp; rg -q 'Windows' .planning/phases/03-core-annotation-tools/03-VALIDATION.md &amp;&amp; rg -q 'phase3-tools' .planning/phases/03-core-annotation-tools/03-VALIDATION.md</automated>
    <fails_when>non-zero exit; no Windows runner/manual path exists; the full phase3-tools command is absent; logs/results are not timestamped and retained; host identity is absent; the validation artifact merges macOS and Windows rows; or the plan claims Windows PASS without an execution result.</fails_when>
  </verify>
  <acceptance_criteria>The workflow and manual instructions define a Windows-only full phase3-tools execution, timestamped host metadata, retained logs/results, six separate parity groups, and a distinct validation-evidence handoff for Task 03-09-03.</acceptance_criteria>
  <done>The Windows configuration is executable and fail-closed for non-Windows or skipped matrix groups; this task does not close G-03-21 by configuration inspection alone.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 03-09-03: Execute Windows phase3-tools and close G-03-21 from retained host evidence</name>
  <read_first>
    - `.planning/phases/03-core-annotation-tools/03-UAT.md` — G-03-21's required Windows parity outcome and six behavior groups.
    - `.planning/phases/03-core-annotation-tools/03-VERIFICATION.md` — the rule that Windows cannot be inferred from macOS and the exact native verification boundary.
    - `.planning/phases/03-core-annotation-tools/03-VALIDATION.md` — the separate Windows rows and evidence fields that may be updated only after a green host run.
    - `.planning/debug/g-03-21-windows-parity.md` — the diagnosed absence of a Windows runtime result and the reason cross-compilation is not evidence.
    - `.github/workflows/phase3-native.yml` — the configured Windows job, artifact names, and required matrix command from Task 03-09-02.
  </read_first>
  <precondition>A GitHub-authenticated Windows 2022 runner or Windows 10 22H2+ host is available and can execute the configured workflow/manual command; a macOS shell or cross-target compile is not an eligible host.</precondition>
  <what-built>The configured Windows workflow/manual path has produced a candidate phase3-tools run and retained evidence bundle for the current revision.</what-built>
  <how-to-verify>Inspect the Windows run and artifact bundle named by the automated verification. Confirm the run is green on a real Windows host, the full phase3-tools matrix has no skipped group, timestamps and host metadata are present, and each separate Windows validation row names the retained evidence.</how-to-verify>
  <resume-signal>Approve only after the Windows evidence gate is green; otherwise report the missing artifact, host field, parity group, or validation row and keep G-03-21 open.</resume-signal>
  <action>Run the configured Windows job on an actual Windows host for the current revision. For CI, dispatch `.github/workflows/phase3-native.yml` with the Phase 3 tools run enabled, resolve the run ID, wait for the Windows job to finish, require the job conclusion and `pnpm exec wdio run wdio.conf.ts --suite phase3-tools` exit status to be zero, then download the retained artifact bundle. For a manual host, execute the same frozen-install, test, build, debug-Tauri, and phase3-tools sequence in PowerShell. Require the evidence bundle to contain UTC start/end timestamps, OS version/build, runner or device identity, WebView/runtime identity, revision, exact command output and exit status, and retained WebDriver/native logs plus screenshots or result JSON for pointer preview/commit, text/IME, eraser, click-through, style/fill, lifecycle, and generated-overlay targeting. Update `03-VALIDATION.md` only after the Windows run is green: add distinct Windows rows for each required behavior, link or name the retained timestamped artifacts and host metadata, record the actual command/result, and leave every row unclosed if any required group is skipped or fails. The acceptance gate is fail-closed: absent artifacts, unknown host metadata, a nonzero or cancelled run, a pending/not-run result, or copied macOS evidence means this task is incomplete and G-03-21 remains open. Keep tray process persistence expected after runner completion unless Quit was explicitly invoked.
  <verify>
    <automated>test -n "${WINDOWS_PHASE3_RUN_ID:-}" &amp;&amp; gh run watch "$WINDOWS_PHASE3_RUN_ID" --exit-status &amp;&amp; gh run view "$WINDOWS_PHASE3_RUN_ID" --json conclusion,headSha,jobs &gt; .planning/phases/03-core-annotation-tools/windows-phase3-run.json &amp;&amp; gh run download "$WINDOWS_PHASE3_RUN_ID" --name windows-phase3-tools-evidence --dir .planning/phases/03-core-annotation-tools/windows-phase3-tools-evidence &amp;&amp; test -s .planning/phases/03-core-annotation-tools/windows-phase3-run.json &amp;&amp; test -d .planning/phases/03-core-annotation-tools/windows-phase3-tools-evidence &amp;&amp; rg -q 'success' .planning/phases/03-core-annotation-tools/windows-phase3-run.json &amp;&amp; rg -q 'Windows|windows' .planning/phases/03-core-annotation-tools/windows-phase3-tools-evidence/* &amp;&amp; rg -q 'UTC|timestamp|OS|runner|device|exit' .planning/phases/03-core-annotation-tools/windows-phase3-tools-evidence/* &amp;&amp; rg -q '03-09-03.*Windows|Windows.*03-09-03' .planning/phases/03-core-annotation-tools/03-VALIDATION.md &amp;&amp; rg -q 'Windows.*PASS|PASS.*Windows' .planning/phases/03-core-annotation-tools/03-VALIDATION.md</automated>
    <human-check>Open the downloaded Windows artifact bundle and confirm its host metadata and timestamps identify a real Windows runner, the full `phase3-tools` result is green with no skipped parity group, and each separate Windows validation row points to that bundle rather than to macOS output. Confirm that any lifecycle persistence observation is classified according to whether Quit was invoked.</human-check>
  </verify>
  <acceptance_criteria>G-03-21 is closed only by a green Windows-host `phase3-tools` execution with nonempty retained timestamped artifacts, explicit Windows host/platform metadata, command exit status, and separate validation rows for pointer, text/IME, eraser, click-through, style, lifecycle, and generated-overlay targeting.</acceptance_criteria>
  <done>The actual Windows run is green, its retained artifact bundle and host metadata are attached or named in `03-VALIDATION.md`, every required Windows parity row is evidence-backed, and no row relies on `NOT RUN`, `PENDING`, or macOS inference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WDIO guest bridge -> Tauri command/window APIs | Test code crosses from the embedded webview into native lifecycle and scene commands. |
| Window discovery -> native pointer/keyboard input | A selected label determines which webview receives W3C actions; hidden bootstrap and visible generated overlays are distinct surfaces. |
| Windows host -> retained evidence | Native results, logs, timestamps, and host identity must remain tied to the Windows runtime that produced them. |
| Tray lifecycle -> test cleanup | Closing/hiding windows does not imply process exit; only explicit Quit is an exit assertion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09-01 | Tampering | WDIO bridge readiness and invoke path | high | mitigate | Require guest bridge readiness before commands, include bridge/window diagnostics, and fail before gesture assertions when readiness is false. |
| T-03-09-02 | Spoofing/Tampering | Overlay window target selection | high | mitigate | Enumerate after Show, select only visible `overlay-display-*` labels, verify lifecycle/canvas readiness after switch, and reject the hidden bootstrap as the drawing target. |
| T-03-09-03 | Tampering | Native pointer/text evidence | high | mitigate | Assert preview-before-up, exact-one commit, draft visibility, keyboard semantics, and scene-count isolation on the real selected window. |
| T-03-09-04 | Information Disclosure/Tampering | Cross-platform validation ledger | high | mitigate | Store timestamped OS/runner metadata and separate Windows artifacts/rows; prohibit macOS-to-Windows inference. |
| T-03-09-05 | Denial of Service | Tray-owned process lifecycle | medium | mitigate | Treat persistence after close/runner completion as expected and reserve process-exit assertions for an explicit Quit action. |
| T-03-SC | Tampering | Dependency/package surface | low | accept | Use the repository's locked WDIO/Tauri tooling and existing dependency-management path; no unrelated package surface is introduced. |
</threat_model>

<verification>
- macOS automated gate: frontend regression/build plus short-lifecycle, phase3-tools, and phase1-matrix native runs, each targeting a visible generated overlay-display-* window and recording bridge/window diagnostics.
- Windows automated gate: a Windows-hosted full setup/build plus `pnpm exec wdio run wdio.conf.ts --suite phase3-tools`, with retained timestamped logs/results and host metadata.
- Evidence gate: `03-VALIDATION.md` contains separate macOS and Windows rows for pointer, text/IME, eraser, click-through, style, lifecycle, and generated-overlay targeting; a missing Windows host keeps the Windows row NOT RUN/PENDING.
- Lifecycle gate: tray process persistence is expected after close or runner completion unless the test explicitly invokes Quit.
</verification>

<success_criteria>
- G-03-18: native text placement reaches the real overlay, shows the draft, commits with Enter, inserts a newline with Shift+Enter, and cancels with Esc without mutating retained scene data.
- G-03-20: the supported WDIO guest bridge is ready, Show selects the visible generated overlay-display-* target, and macOS native pointer/lifecycle suites complete without targeting hidden bootstrap state.
- G-03-21: Windows parity is evidenced by a real Windows execution/manual path with timestamped host-level results for pointer, text/IME, eraser, click-through, style, and lifecycle; macOS cannot satisfy the Windows row.
</success_criteria>

<source_audit>
| Source | ID | Feature/decision | Plan | Status |
|--------|----|------------------|------|--------|
| GOAL | — | Phase 3 users create core annotations quickly on the shared overlay | 03-06, 03-08, 03-09 | COVERED |
| REQ | DRAW-01 | Pen/freehand drawing | 03-06, 03-09 | COVERED |
| REQ | DRAW-02 | Highlighter drawing | 03-06, 03-09 | COVERED |
| REQ | DRAW-03 | Lines and arrows | 03-06, 03-09 | COVERED |
| REQ | DRAW-04 | Rectangle/ellipse fill and opacity | 03-06, 03-09 | COVERED |
| REQ | DRAW-05 | Text draft, commit, cancel, and keyboard flow | 03-06, 03-09 | COVERED |
| REQ | DRAW-06 | Single-item eraser behavior | 03-06, 03-09 | COVERED |
| RESEARCH | native bridge/window constraints | 03-09 | COVERED |
| CONTEXT | D-06, D-10 | Transient preview and cancellation | 03-06, 03-09 | COVERED |
| CONTEXT | D-11, D-12, D-13 | Text placement and keyboard lifecycle | 03-03, 03-06, 03-09 | COVERED |
| CONTEXT | D-14, D-15, D-16, D-17 | Eraser click/topmost/type-specific hover | 03-03, 03-06, 03-09 | COVERED |
| CONTEXT | Deferred Ideas | Undo/export/persistence/post-commit text editing | excluded | EXCLUDED |
</source_audit>

<output>
The planner creates only `.planning/phases/03-core-annotation-tools/03-09-PLAN.md`; execution must update only the listed implementation/test/CI/evidence paths and produce its normal execution summary through the existing GSD workflow.
</output>
