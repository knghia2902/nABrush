---
phase: 03-core-annotation-tools
plan: 09
type: execute
wave: 9
depends_on: [03-06, 03-08]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/main.tsx
  - src-tauri/tauri.conf.json
  - src-tauri/Cargo.toml
  - src-tauri/Cargo.lock
  - src-tauri/src/main.rs
  - src-tauri/capabilities/default.json
  - wdio.conf.ts
  - tests/e2e/overlay.e2e.ts
  - tests/e2e/core-annotation-tools.e2e.ts
autonomous: false
gap_closure: true
gap_ids: [G-03-18, G-03-20]
requirements: [DRAW-01, DRAW-02, DRAW-03, DRAW-04, DRAW-05, DRAW-06]
estimate:
  tokens: 14000
  raw_tokens: 11500
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "On macOS, the supported WDIO Tauri guest bridge is initialized and awaited before native commands, window enumeration, or target switching."
    - "After Show, native smoke targets a visible generated overlay-display-* window, verifies its canvas and lifecycle state, and never treats the hidden bootstrap overlay as the drawing surface."
    - "The generated macOS target accepts real pointer and keyboard input: preview appears before pointer-up, a valid gesture commits exactly one retained item, and text draft Enter/Shift+Enter/Esc behavior is observable without scene pollution."
    - "The tray-owned process may remain alive after a runner or window close; process exit is required only after an explicit Quit assertion."
  artifacts:
    - "wdio.conf.ts and the WDIO/Tauri guest setup expose awaited bridge readiness and generated-overlay discovery diagnostics."
    - "tests/e2e/overlay.e2e.ts and tests/e2e/core-annotation-tools.e2e.ts exercise the generated overlay-display-* target for lifecycle, pointer, and text input."
  key_links:
    - "Guest bridge readiness -> Show/reconcile -> visible overlay-display-* handle -> W3C pointer/keyboard input -> SceneSnapshot assertions."
    - "Generated-window discovery is shared by phase3-tools, short-lifecycle, and phase1-matrix; the static hidden overlay remains bootstrap-only."
---

<objective>
Close G-03-18 and G-03-20 on macOS by making the supported Tauri WebDriver bridge ready before use and driving native assertions against the visible generated overlay window.

Purpose: The diagnosed failure is at the bridge/window/input boundary, not in the already-tested retained scene implementation.
Output: A deterministic macOS bridge/readiness helper and passing native lifecycle, pointer, and text draft flows on the generated overlay target.
</objective>

<execution_context>
@/Users/khacnghia/.codex/gsd-core/workflows/execute-plan.md
@/Users/khacnghia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-annotation-tools/03-CONTEXT.md
@.planning/phases/03-core-annotation-tools/03-RESEARCH.md
@.planning/phases/03-core-annotation-tools/03-PATTERNS.md
@.planning/phases/03-core-annotation-tools/03-UAT.md
@.planning/phases/03-core-annotation-tools/03-VERIFICATION.md
@.planning/phases/03-core-annotation-tools/03-VALIDATION.md
@.planning/phases/03-core-annotation-tools/03-06-PLAN.md
@.planning/debug/text-tool-draft-g-03-18.md
@.planning/debug/g-03-20-native-lifecycle.md
</context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 03-09-00: Verify the newly published WDIO bridge package before installation</name>
  <read_first>
    - `node_modules/@wdio/tauri-service/package.json` — installed service version and package metadata.
    - `node_modules/@wdio/tauri-service/docs/plugin-setup.md` — exact `@wdio/tauri-plugin` import and paired Rust plugin requirements.
    - `.planning/phases/03-core-annotation-tools/03-RESEARCH.md` — package legitimacy policy and the original no-new-package assumption superseded by this diagnosed bridge blocker.
  </read_first>
  <what-built>The checked-in service documentation and package metadata identify the exact JS bridge package and its paired Rust/runtime requirements.</what-built>
  <how-to-verify>Verify the package identity command and inspect the service docs, then explicitly approve the SUS package before Task 03-09-01 changes package manifests.</how-to-verify>
  <resume-signal>Approve only the exact WebdriverIO-maintained `@wdio/tauri-plugin`; otherwise keep the bridge task blocked and do not substitute another package.</resume-signal>
  <action>Confirm that the registry package named exactly `@wdio/tauri-plugin` is the WebdriverIO-maintained package paired with the repository's `@wdio/tauri-service@1.4.0`, verify its package identity and version before adding it, and approve installation only for this bridge gap. Do not substitute an unverified package or generic bridge.</action>
  <verify>
    <automated>node /Users/khacnghia/.codex/gsd-core/bin/gsd-tools.cjs query package-legitimacy check --ecosystem npm @wdio/tauri-plugin</automated>
    <fails_when>The command exits non-zero, the package does not resolve as `@wdio/tauri-plugin`, its repository identity does not match the WebdriverIO desktop-mobile package, or blocking human approval is absent.</fails_when>
  </verify>
  <acceptance_criteria>The exact `@wdio/tauri-plugin` package is identity-verified and explicitly approved before the dependency/install changes in Task 03-09-01 proceed.</acceptance_criteria>
  <done>Package legitimacy approval is recorded and the executor may install only the exact documented bridge package.</done>
</task>

<task type="tracer">
  <name>Task 03-09-01: Initialize the macOS WDIO bridge and discover the visible generated overlay</name>
  <read_first>
    - `.planning/phases/03-core-annotation-tools/03-PATTERNS.md` — established native/window evidence patterns; explicitly preserve the intentional deviation from the prior static-label analog because visible runtime surfaces are generated `overlay-display-*` windows and generated-window discovery is required by the diagnosed bug.
    - `.planning/phases/03-core-annotation-tools/03-CONTEXT.md` — D-01, D-02, D-03, D-06, D-10, D-11, D-12, and D-13.
    - `.planning/debug/g-03-20-native-lifecycle.md` — diagnosed missing guest bridge, hidden bootstrap label, generated visible labels, and intentional tray persistence.
    - `wdio.conf.ts`, `src/main.tsx`, `src-tauri/tauri.conf.json`, `src-tauri/capabilities/default.json`, `tests/e2e/overlay.e2e.ts` — current bridge/bootstrap, permissions, and lifecycle helpers.
  </read_first>
  <behavior>
    - The exact supported JS bridge is `@wdio/tauri-plugin` paired with the installed `@wdio/tauri-service@1.4.0`; `src/main.tsx` contains the literal side-effect import `import "@wdio/tauri-plugin"`, while Rust registers `tauri_plugin_wdio` and retains the installed `tauri-plugin-wdio-webdriver` embedded-provider plugin.
    - `src-tauri/tauri.conf.json` sets `app.withGlobalTauri` to `true`, capabilities grant `wdio:default`, and readiness explicitly proves `window.__wdio_original_core__` exists before `browser.tauri.execute`, application invoke, window enumeration, or switching.
    - Readiness diagnostics include host identity, bridge state, current labels, and selected label; failure stops before gesture assertions.
    - After Show, discovery filters for a visible `overlay-display-*` label, verifies document/canvas readiness and lifecycle mode on that handle, and retains static `overlay` only as bootstrap state.
    - The intentional deviation is documented: the prior static overlay-label analog is insufficient because visible runtime surfaces are generated `overlay-display-*`; generated-window discovery is required by the diagnosed bug.
  </behavior>
  <files>package.json, pnpm-lock.yaml, src/main.tsx, src-tauri/tauri.conf.json, src-tauri/Cargo.toml, src-tauri/Cargo.lock, src-tauri/src/main.rs, src-tauri/capabilities/default.json, wdio.conf.ts, tests/e2e/overlay.e2e.ts</files>
  <action>Use the exact bridge documented by the checked-in `node_modules/@wdio/tauri-service` package: add `@wdio/tauri-plugin` to `package.json`/`pnpm-lock.yaml`, add the literal side-effect import `import "@wdio/tauri-plugin"` in `src/main.tsx`, register `tauri_plugin_wdio::init()` in `src-tauri/src/main.rs`, retain `tauri-plugin-wdio-webdriver = "1"` for the embedded provider, set `app.withGlobalTauri` to `true` in `src-tauri/tauri.conf.json`, and grant `wdio:default` in capabilities. The helper must explicitly poll `window.__wdio_original_core__` and fail before native commands when it is absent, then wait for bootstrap, invoke Show, enumerate labels, select only a visible generated `overlay-display-*` window, switch to it, and recheck canvas/lifecycle readiness. Replace hard-coded visible-target assumptions in short-lifecycle and phase1-matrix while retaining bootstrap access where needed. Preserve the normal command bridge and tray-owned lifecycle; process exit is asserted only after explicit Quit. This intentionally deviates from the prior static overlay-label analog because generated-window discovery is required by the diagnosed bug.</action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm exec wdio run wdio.conf.ts --suite short-lifecycle &amp;&amp; pnpm exec wdio run wdio.conf.ts --suite phase1-matrix</automated>
    <fails_when>Any command exits non-zero; `@wdio/tauri-plugin` is not the imported bridge; `tauri_plugin_wdio::init()`, `wdio:default`, or `withGlobalTauri: true` is absent; `window.__wdio_original_core__` is not checked and awaited; diagnostics omit host/bridge/labels; Show has no visible generated target; hidden bootstrap `overlay` is selected as drawing target; post-switch canvas/lifecycle readiness is absent; or lifecycle setup requires tray-process exit without explicit Quit.</fails_when>
  </verify>
  <acceptance_criteria>The macOS runner uses the exact `@wdio/tauri-plugin` import with `tauri_plugin_wdio::init()`, `wdio:default`, and `withGlobalTauri: true`; it awaits `window.__wdio_original_core__`, discovers and switches to a visible generated overlay-display-* window after Show, applies the contract to both lifecycle suites, and records the static-label deviation and tray persistence semantics.</acceptance_criteria>
  <done>G-03-20 bridge, target identity, readiness, and lifecycle setup conditions are satisfied for macOS without changing scene behavior.</done>
</task>

<task type="auto">
  <name>Task 03-09-02: Prove macOS pointer delivery and text draft lifecycle on the generated target</name>
  <read_first>
    - `.planning/phases/03-core-annotation-tools/03-PATTERNS.md` — retained-scene, transient-preview, text-draft, and native evidence patterns, including the generated-window deviation.
    - `.planning/phases/03-core-annotation-tools/03-UAT.md` — G-03-18 Test 18 and G-03-20 Test 20 expected outcomes.
    - `.planning/phases/03-core-annotation-tools/03-VERIFICATION.md`, `.planning/debug/text-tool-draft-g-03-18.md`, `.planning/debug/g-03-20-native-lifecycle.md` — native evidence boundary and diagnosed wrong-target/input path.
    - `.planning/phases/03-core-annotation-tools/03-06-PLAN.md`, `wdio.conf.ts`, `tests/e2e/core-annotation-tools.e2e.ts` — existing preview/commit/cancel and tool assertions.
  </read_first>
  <behavior>
    - W3C pointer input on the discovered generated target shows transient preview before pointer-up and adds exactly one retained item after a valid pointer-up.
    - Native text placement visibly creates and focuses the scene-excluded draft; non-composing Enter commits, Shift+Enter inserts a newline without committing, and Esc cancels without changing retained scene count.
    - Existing tool selection, independent style/fill, eraser topmost/click-only, click-through, and cancellation assertions remain in the generated-target flow.
  </behavior>
  <files>wdio.conf.ts, tests/e2e/core-annotation-tools.e2e.ts, tests/e2e/overlay.e2e.ts</files>
  <action>Extend the existing Phase 3 smoke flow to use the readiness helper's visible generated target for every W3C pointer and keyboard action. Assert preview before pointer-up, exactly one retained commit, text draft visibility/focus, non-composing Enter commit, Shift+Enter newline, Esc cancellation with unchanged scene count, and existing tool/style/fill/eraser/click-through/cancellation behavior. Keep editor and diagnostics scene-excluded, record host/generated-window identity, and honor D-06, D-10, D-11 through D-13, and D-14 through D-17.</action>
  <verify>
    <automated>pnpm exec vitest run src/components/overlay-surface.test.tsx &amp;&amp; pnpm exec wdio run wdio.conf.ts --suite phase3-tools</automated>
    <fails_when>Any command exits non-zero; selected label is not a visible generated overlay-display-* window; preview is absent before pointer-up; valid gesture does not commit exactly one item; text draft/focus, Enter, Shift+Enter, or Esc fails; existing tool/style/fill/eraser/click-through/cancellation regresses; or retained scene data includes transient/editor state.</fails_when>
  </verify>
  <acceptance_criteria>macOS phase3-tools reaches the real generated overlay and passes native pointer preview/commit plus text draft Enter/Shift+Enter/Esc lifecycle while preserving existing Phase 3 assertions and scene semantics.</acceptance_criteria>
  <done>G-03-18 is closed and the macOS native portion of G-03-20 is evidenced by a passing generated-overlay phase3-tools run; no Windows status changes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| WebDriver host -> Tauri guest | Native test commands and pointer/keyboard input cross into the app webview. |
| Tauri bootstrap -> generated overlay | Lifecycle state and window identity cross from hidden bootstrap to per-display runtime surfaces. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09-01 | Tampering | WDIO guest bridge | high | mitigate | Await bridge readiness and fail before native assertions when diagnostics are incomplete. |
| T-03-09-02 | Spoofing | Window target discovery | high | mitigate | Select only visible generated `overlay-display-*` labels and verify canvas/lifecycle after switching. |
| T-03-09-03 | Tampering | Pointer/text native evidence | high | mitigate | Assert preview-before-up, exact-one commit, draft visibility, keyboard semantics, and scene isolation. |
| T-03-09-04 | Denial of Service | Tray-owned lifecycle | medium | mitigate | Treat persistence after close/runner completion as expected; reserve exit checks for explicit Quit. |
| T-03-SC | Tampering | Existing WDIO/Tauri packages | low | accept | Use locked repository dependencies and no unrelated package surface. |
</threat_model>

<verification>
- `pnpm build`, focused renderer tests, short-lifecycle, phase1-matrix, and phase3-tools pass on macOS.
- All suites use awaited bridge readiness and visible generated-overlay discovery.
</verification>

<success_criteria>
- G-03-18: macOS native text placement reaches the real overlay, shows the draft, commits with Enter, inserts a newline with Shift+Enter, and cancels with Esc without mutating retained scene data.
- G-03-20: supported bridge is ready, Show selects a visible generated overlay-display-* target, and macOS native suites no longer operate on hidden bootstrap.
</success_criteria>

<source_audit>
| Source | ID | Feature/decision | Plan | Status |
|--------|----|------------------|------|--------|
| GOAL | — | Presenter creates core annotations smoothly on the overlay | 03-09 | COVERED |
| REQ | DRAW-01..DRAW-06 | Native proof for all Phase 3 tools | 03-09 | COVERED |
| RESEARCH | Native bridge/generated-window constraints | 03-09 | COVERED |
| CONTEXT | D-01..D-17 applicable interaction rules | 03-09 | COVERED |
| CONTEXT | Deferred Ideas | undo/export/persistence/post-commit editing | excluded | EXCLUDED |
</source_audit>

<output>
Create `.planning/phases/03-core-annotation-tools/03-09-SUMMARY.md` when done.
</output>
