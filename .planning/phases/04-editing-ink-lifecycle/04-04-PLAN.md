---
phase: 04-editing-ink-lifecycle
plan: 04
type: execute
gap_closure: true
gap_ids:
  - G-04-2
wave: 4
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - src/App.tsx
  - src/types/overlay.ts
  - src/components/AnnotationToolbar.tsx
  - src/components/annotation-toolbar.test.tsx
  - src/styles.css
  - src-tauri/src/main.rs
  - tests/fixtures/shortcut-underlay.html
  - tests/e2e/editing-ink-lifecycle.e2e.ts
  - wdio.conf.ts
autonomous: false
requirements:
  - EDIT-01
user_setup:
  - "Chạy WDIO và kiểm thử phím thật trên Windows; xác nhận Undo/Redo và bộ đếm phím của ứng dụng nền trước khi đóng G-04-2."
  - "Sau thay đổi toolbar, xác nhận bằng mắt toolbar chỉ còn biểu tượng trên macOS/Windows."
estimate:
  tokens: 7500
  raw_tokens: 15000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "Trên Windows, Ctrl+Z thực hiện Undo, Ctrl+Y thực hiện Redo, không gọi Clear All và không làm ứng dụng bên dưới nhận hai phím đó."
    - "Toolbar chỉ hiển thị biểu tượng; mỗi nút vẫn có accessible name và tooltip, các popover vẫn có nhãn/giá trị cần thiết."
  artifacts:
    - path: "tests/fixtures/shortcut-underlay.html"
      provides: "Ứng dụng nền kiểm thử có bộ đếm nhìn thấy được cho Ctrl/Cmd+Z và Ctrl+Y."
    - path: "tests/e2e/editing-ink-lifecycle.e2e.ts"
      provides: "Desktop regression cho scene Undo/Redo và định tuyến shortcut theo host."
    - path: "src/components/AnnotationToolbar.tsx"
      provides: "Toolbar icon-only với tooltip và accessible name."
    - path: ".planning/phases/04-editing-ink-lifecycle/04-04-SUMMARY.md"
      provides: "Bằng chứng host/commit riêng cho shortcut và toolbar."
---

<objective>
Đóng G-04-2 bằng kiểm tra Undo/Redo có oracle cho cả scene lẫn ứng dụng bên dưới; đồng thời hoàn tất yêu cầu người dùng muốn toolbar chỉ hiện icon. Không suy ra Windows pass từ macOS.
</objective>

<context>
- Đọc `.planning/phases/04-editing-ink-lifecycle/04-UAT.md`, `.planning/debug/windows-undo-redo-shortcuts.md`, `.planning/phases/04-editing-ink-lifecycle/04-CONTEXT.md`, các plan `04-01` đến `04-03`, `.planning/STATE.md`, `src/App.tsx`, `src/types/overlay.ts`, `src-tauri/src/main.rs`, `src/components/AnnotationToolbar.tsx`, test và `wdio.conf.ts`.
- Điều tra hiện tại cho thấy helper frontend ánh xạ Ctrl+Z/Ctrl+Y sang Undo/Redo và native handlers riêng; build/đường focus thực trên Windows chưa được kiểm chứng.
- UAT bổ sung yêu cầu toolbar icon-only. Giữ chức năng/keyboard/drag/clamp và accessibility; không thêm dependency UI.
</context>

<tasks>

<task type="tracer">
  <name>Task 04-04-01: Xác minh phím tắt trên Windows bằng scene và sentinel ứng dụng nền</name>
  <read_first>.planning/phases/04-editing-ink-lifecycle/04-UAT.md, .planning/debug/windows-undo-redo-shortcuts.md, .planning/phases/04-editing-ink-lifecycle/04-CONTEXT.md, src/App.tsx, src/types/overlay.ts, src-tauri/src/main.rs, tests/e2e/editing-ink-lifecycle.e2e.ts, wdio.conf.ts</read_first>
  <files>src/App.tsx, src/types/overlay.ts, src-tauri/src/main.rs, tests/fixtures/shortcut-underlay.html, tests/e2e/editing-ink-lifecycle.e2e.ts, wdio.conf.ts</files>
  <action>Tạo fixture HTML cục bộ làm ứng dụng nền, với counter hiển thị và `window.__receivedShortcutEvents`; counter chỉ tăng khi nhận keydown Ctrl/Cmd+Z hoặc Ctrl+Y và hiển thị key/modifiers để người kiểm thử đọc sau khi ẩn overlay. Trên WDIO, tạo hai trạng thái scene dễ phân biệt, gửi Ctrl+Z rồi Ctrl+Y qua đường keyboard của overlay, kiểm tra snapshot canonical sau mỗi phím, ghi target/focus/modifiers và native command trace, đồng thời assert không gọi `clear_scene`. Trên cả Windows và macOS, chạy kiểm thử thủ công bổ sung bằng cách mở fixture trong trình duyệt nằm dưới overlay, ghi nhận counter ban đầu bằng 0, đưa overlay lên trước, dùng phím thật khi overlay đang nhận input, rồi ẩn overlay và đọc counter; counter phải vẫn bằng 0. Trên Windows ghi rõ SHA của checkout và phiên bản/build được chạy; nếu hành vi hiện tại đã đúng, không đổi mapping chỉ để tạo diff. Chỉ sửa theo nhánh được trace xác nhận. Không đánh dấu task hoặc G-04-2 pass nếu thiếu kết quả Windows, kiểm tra phím thật, hoặc sentinel ứng dụng nền; nếu môi trường không cho đo thì ghi gap còn mở và yêu cầu chạy lại ở host phù hợp.</action>
  <verify>
    <automated>pnpm exec vitest run src/state/annotation.test.ts src/components/annotation-toolbar.test.tsx</automated>
    <fails_when>Helper hoặc wiring không phân biệt Ctrl/Cmd+Z, Ctrl+Y, trạng thái history và Clear All riêng.</fails_when>
    <automated>pnpm exec wdio run wdio.conf.ts --suite phase4-editing</automated>
    <fails_when>Trên từng host chạy suite, snapshot không Undo/Redo đúng hoặc `clear_scene` bị gọi bởi shortcut.</fails_when>
    <automated>pnpm build</automated>
    <fails_when>Frontend/Tauri command integration không build được sau thay đổi.</fails_when>
  </verify>
  <acceptance_criteria>
    - Lưu hai WDIO report độc lập — một cho Windows, một cho macOS — mỗi report ghi đúng host và commit/build, Ctrl+Z → Undo, Ctrl+Y → Redo trên canonical scene, snapshot trước/sau và call trace chứng minh `clear_scene` không xuất hiện. Không dùng kết quả của host này thay cho host kia.
    - Kiểm thử phím thật với fixture được chạy trên Windows: counter ứng dụng nền vẫn bằng 0 sau cả hai tổ hợp; lưu kết quả cùng host/commit. Chạy phép thử tương tự trên macOS nếu có host và ghi riêng.
    - Nếu Windows hoặc sentinel chưa được chạy/đo, G-04-2 giữ trạng thái failed/open và không được mô tả là đã đóng.
  </acceptance_criteria>
  <done>G-04-2 chỉ hoàn tất khi có bằng chứng Windows cho scene Undo/Redo, Clear All không bị gọi, và sentinel ứng dụng nền vẫn bằng 0 sau phím thật; thiếu bằng chứng thì giữ gap mở.</done>
</task>

<task type="auto">
  <name>Task 04-04-02: Chuyển các nút toolbar sang icon-only</name>
  <read_first>src/components/AnnotationToolbar.tsx, src/components/annotation-toolbar.test.tsx, src/styles.css, .planning/phases/04-editing-ink-lifecycle/04-UAT.md, .planning/phases/04-editing-ink-lifecycle/04-CONTEXT.md</read_first>
  <files>src/components/AnnotationToolbar.tsx, src/components/annotation-toolbar.test.tsx, src/styles.css</files>
  <action>Đổi các nút trực tiếp trên toolbar sang chỉ có icon, gồm tool, Undo/Redo, lifecycle và hành động hiện có; ẩn nhãn chữ trực quan mà không tăng kích thước toolbar. Giữ `aria-label`/accessible name và tooltip `title` bằng tiếng Việt rõ ràng. Giữ trạng thái disabled, callback, thứ tự nút, drag handle, clamp viewport, scene exclusion, keyboard behavior và popover hiện tại. Các giá trị bên trong popover như preset thời lượng vẫn giữ phần chữ cần thiết. Dùng icon system sẵn có, không thêm dependency. Cập nhật test để xác nhận nhãn toolbar không hiển thị, mọi nút có accessible name/tooltip và callback/selector không đổi.</action>
  <verify>
    <automated>pnpm exec vitest run src/components/annotation-toolbar.test.tsx</automated>
    <fails_when>Toolbar còn nhãn chữ trực tiếp, thiếu accessible name/tooltip, hoặc làm đổi callback, trạng thái disabled, drag/clamp hay scene exclusion.</fails_when>
    <automated>pnpm build</automated>
    <fails_when>Toolbar icon-only không type-check hoặc production build lỗi.</fails_when>
  </verify>
  <acceptance_criteria>
    - Hàng toolbar chỉ hiển thị biểu tượng, bao gồm lifecycle; tên công cụ/hành động không hiện thành chữ trên các nút trực tiếp.
    - Tên truy cập và tooltip tiếng Việt vẫn có; popover giữ nhãn/giá trị cần thiết.
    - Drag, clamp, click-through, Undo/Redo, callback công cụ và scene exclusion không đổi.
  </acceptance_criteria>
  <done>Toolbar icon-only hoạt động và truy cập được; test component và production build pass, kiểm tra trực quan được ghi riêng theo host.</done>
</task>

</tasks>

<verification>
Chạy Vitest/build một lần trên checkout hiện tại và phase4-editing WDIO riêng trên Windows cùng macOS; lưu mỗi kết quả với OS, commit/build, scene snapshots và command trace. Xác nhận fixture ứng dụng nền bằng phím thật trên Windows; ghi counter trước/sau, và ghi riêng macOS nếu đã chạy. Sau khi task hoàn tất, cập nhật summary và chạy `$gsd-verify-work 04`; không đánh dấu gap pass khi thiếu bước người dùng/host tương ứng.
</verification>

<success_criteria>
G-04-2 có bằng chứng trên Windows rằng Ctrl+Z/Ctrl+Y undo/redo, không gọi Clear All và không tác động ứng dụng bên dưới; toolbar hiển thị icon-only mà vẫn giữ accessibility và thao tác. Thiếu kiểm tra Windows/sentinel thì gap và phase vẫn chưa hoàn tất.
</success_criteria>

<output>
Tạo `.planning/phases/04-editing-ink-lifecycle/04-04-SUMMARY.md` sau khi thực thi; ghi rõ gap nào còn mở nếu chưa có bằng chứng host.
</output>
