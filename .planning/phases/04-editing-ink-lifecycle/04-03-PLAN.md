---
phase: 04-editing-ink-lifecycle
plan: 03
type: execute
gap_closure: true
gap_ids:
  - G-04-1
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/App.tsx
  - src/components/OverlaySurface.tsx
  - src/components/overlay-surface.test.tsx
  - src-tauri/src/main.rs
  - tests/e2e/editing-ink-lifecycle.e2e.ts
  - wdio.conf.ts
autonomous: false
requirements:
  - EDIT-03
  - EDIT-04
  - EDIT-05
user_setup:
  - "Sau khi có bản sửa, chạy lại đúng tình huống UAT Vanishing trên mỗi host liên quan và xác nhận pass/issue; nếu không thể tái hiện thì giữ G-04-1 mở."
estimate:
  tokens: 6500
  raw_tokens: 13000
  tasks: 1
  confidence: medium
must_haves:
  truths:
    - "Nét Vanishing giữ nguyên hình học khi chọn lifecycle, fade liên tục trong giây cuối ở trạng thái click-through, hết hạn đúng hạn và không xuất hiện lại khi bật tương tác."
  artifacts:
    - path: "tests/e2e/editing-ink-lifecycle.e2e.ts"
      provides: "Desktop regression cho alpha/fade/expiry ở VisibleClickThrough và tọa độ nét khi đổi lifecycle."
    - path: "src/components/OverlaySurface.tsx"
      provides: "Đường render lifecycle và gesture có thể đối chiếu với timestamp, pointer samples và viewport."
    - path: ".planning/phases/04-editing-ink-lifecycle/04-03-SUMMARY.md"
      provides: "Bằng chứng riêng theo host/commit và trạng thái gap trung thực."
---

<objective>
Tái hiện và đóng G-04-1 bằng bằng chứng cho cả fade khi click-through lẫn nét bị nhảy; không giả định hai triệu chứng cùng nguyên nhân. Nếu chưa xác nhận được tình huống người dùng báo, giữ gap mở và không tuyên bố phase hoàn tất.
</objective>

<context>
- Đọc `.planning/phases/04-editing-ink-lifecycle/04-UAT.md`, `.planning/debug/vanishing-fade-and-stroke-jump.md`, `.planning/phases/04-editing-ink-lifecycle/04-CONTEXT.md`, hai plan trước, `.planning/STATE.md` và mã nguồn/test liệt kê trong task.
- RCA hiện kết luận inconclusive. Phép tính opacity không tự thay đổi geometry; E2E hiện chưa bao phủ `VisibleClickThrough` hoặc trace pointer/viewport.
- Không log pixel, nội dung màn hình hay toàn văn annotation. Probe chẩn đoán chỉ ở test hoặc gỡ sau khi khoanh vùng.
</context>

<tasks>

<task type="tracer">
  <name>Task 04-03-01: Tái hiện và sửa có bằng chứng lỗi Vanishing</name>
  <read_first>.planning/phases/04-editing-ink-lifecycle/04-UAT.md, .planning/debug/vanishing-fade-and-stroke-jump.md, .planning/phases/04-editing-ink-lifecycle/04-CONTEXT.md, .planning/phases/04-editing-ink-lifecycle/04-01-PLAN.md, .planning/phases/04-editing-ink-lifecycle/04-02-PLAN.md, src/types/overlay.ts, src/App.tsx, src/components/OverlaySurface.tsx, src/components/overlay-surface.test.tsx, tests/e2e/editing-ink-lifecycle.e2e.ts, src-tauri/src/main.rs, wdio.conf.ts</read_first>
  <files>src/App.tsx, src/components/OverlaySurface.tsx, src/components/overlay-surface.test.tsx, src-tauri/src/main.rs, tests/e2e/editing-ink-lifecycle.e2e.ts, wdio.conf.ts</files>
  <action>Thêm hai phép tái hiện độc lập: (1) tạo nét Vanishing, chuyển overlay đang hiển thị sang click-through trước giây fade cuối, lấy mẫu alpha qua nhiều frame, xác nhận alpha giảm đơn điệu đến 0, item bị loại đúng deadline và không quay lại sau khi bật tương tác; (2) bật Vanishing rồi vẽ theo chuỗi pointer cố định, đối chiếu điểm đã chuẩn hóa với scene item và với cùng chuỗi ở Persistent. Trong test ghi trạng thái overlay, timestamp/duration/deadline, thời gian frame, alpha, pointer type/id/client coordinates, canvas rect và viewport. Trước tiên chạy để xác định symptom nào tái hiện và điều kiện tối thiểu. Chỉ sửa đường code mà assertion/trace chứng minh sai; giữ canonical scene, D-08/D-10 và metadata lifecycle tách biệt khỏi hình học. Xóa/giới hạn probe sau khoanh vùng. Nếu không tái hiện được, ghi bằng chứng và điều kiện chưa kiểm tra, không tạo workaround suy đoán. G-04-1 chỉ được đóng khi hồi quy desktop chứng minh bất biến trên host liên quan VÀ người dùng xác nhận đúng tình huống UAT sau bản sửa; nếu thiếu một trong hai, cập nhật kết quả là inconclusive/failed-open và chờ thông tin hoặc xác nhận bổ sung, không báo phase complete.</action>
  <verify>
    <automated>pnpm exec vitest run src/components/overlay-surface.test.tsx src/state/annotation.test.ts</automated>
    <fails_when>Kiểm thử đơn vị không chứng minh được ranh giới fade/expiry, bảo toàn hình học của gesture và bất biến lifecycle.</fails_when>
    <automated>pnpm exec wdio run wdio.conf.ts --suite phase4-editing</automated>
    <fails_when>Trên host đang chạy, test không quan sát alpha fade trong click-through, item hết hạn bị hồi sinh, hoặc scene points lệch khỏi chuỗi pointer khi chỉ đổi lifecycle.</fails_when>
    <automated>pnpm build</automated>
    <fails_when>TypeScript hoặc production build lỗi sau sửa render/gesture.</fails_when>
  </verify>
  <acceptance_criteria>
    - Test bao phủ click-through đang hiển thị (không thay bằng Hidden) từ trước fade đến sau deadline và khi bật lại tương tác.
    - Alpha giữ nguyên trước giây cuối, giảm đơn điệu trong fade; item bị loại tại/sau deadline và không hồi sinh.
    - Cùng chuỗi pointer/viewport tạo cùng scene points ở Persistent và Vanishing; trace phân biệt input, transform và render.
    - Nguyên nhân gốc chỉ được ghi nhận khi assertion tái hiện chứng minh; người dùng xác nhận lại đúng tình huống UAT trên host liên quan trước khi gap đổi sang pass.
    - Nếu không tái hiện hoặc chưa có xác nhận người dùng, G-04-1 tiếp tục mở và Phase 04 không được báo hoàn tất.
  </acceptance_criteria>
  <done>G-04-1 chỉ hoàn tất sau hồi quy pass và người dùng xác nhận đúng tình huống UAT trên host liên quan. Nếu chưa đạt điều kiện đó, ghi bằng chứng/giới hạn nhưng giữ gap failed/open.</done>
</task>

</tasks>

<verification>
Chạy Vitest, build và WDIO trên các host liên quan; ghi OS, commit và kết quả riêng. Sau khi có build sửa, người dùng phải lặp lại thao tác UAT Vanishing để xác nhận trước khi cập nhật gap thành pass.
</verification>

<success_criteria>
Click-through fade/expiry và độ ổn định hình học được kiểm chứng mà không gộp RCA. G-04-1 chỉ đóng khi người dùng xác nhận lại; mọi kết quả chưa tái hiện vẫn được giữ là gap mở.
</success_criteria>

<output>
Tạo `.planning/phases/04-editing-ink-lifecycle/04-03-SUMMARY.md` khi task thực sự hoàn tất; nếu đang chờ xác nhận, ghi rõ gap vẫn mở.
</output>
