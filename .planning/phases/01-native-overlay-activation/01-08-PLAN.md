---
phase: 01-native-overlay-activation
plan: 08
type: execute
wave: 8
depends_on: [01-07]
files_modified:
  - src-tauri/src/tray.rs
  - docs/support-matrix.md
autonomous: true
requirements: [OVLY-01, OVLY-02]
gap_closure: true
gap_ids: [G-01-1]
estimate:
  tokens: 12000
  raw_tokens: 8000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A running macOS build exposes a discoverable nABrush status item using the existing icon asset, while Windows continues to expose the same tray lifecycle."
    - "A reviewer can launch the debug app through the documented Tauri development path, find the tray entry, and exercise Show/Hide without expecting a normal document window."
    - "The tray icon fix preserves the background-hidden startup, tray-only close behavior, and global shortcut activation contract (D-01, D-02, D-04)."
  artifacts:
    - path: "src-tauri/src/tray.rs"
      provides: "TrayIconBuilder registration with the checked-in icon and macOS template rendering."
      contains: "TrayIconBuilder"
    - path: "src-tauri/icons/icon.png"
      provides: "Existing 1024px RGBA tray artwork consumed at compile time."
    - path: "docs/support-matrix.md"
      provides: "Reviewable manual/debug launch instructions and tray-only lifecycle expectations."
      contains: "pnpm tauri:dev"
  key_links:
    - from: "src-tauri/icons/icon.png"
      to: "src-tauri/src/tray.rs"
      via: "compile-time image bytes passed to TrayIconBuilder::icon"
      pattern: "include_bytes"
    - from: "docs/support-matrix.md"
      to: "src-tauri/src/tray.rs"
      via: "manual UAT starts the same Tauri app whose tray registration is being reviewed"
      pattern: "tauri:dev"

---

<objective>
Close UAT gap G-01-1 by making the macOS tray status item visible and making the manual debug launch path unambiguous.

Purpose: The overlay and settings windows intentionally remain hidden at startup and out of the taskbar (D-01, D-04). The missing tray icon makes that tray-only lifecycle look like a failed launch during manual verification, while launching the raw debug executable gives the reviewer no useful app identity. Attach the existing artwork to the native tray registration and document the supported development launch path without changing window visibility or close semantics.
Output: A tray registration that supplies the existing icon with macOS template behavior, plus executable manual UAT instructions in the support matrix.
</objective>

<execution_context>
@/Users/khacnghia/.codex/gsd-core/workflows/execute-plan.md
@/Users/khacnghia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-native-overlay-activation/01-UAT.md
@.planning/phases/01-native-overlay-activation/01-VERIFICATION.md
@.planning/phases/01-native-overlay-activation/01-07-SUMMARY.md
@src-tauri/src/tray.rs
@src-tauri/tauri.conf.json
@package.json
@docs/support-matrix.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the existing tray artwork with macOS template behavior</name>
  <files>src-tauri/src/tray.rs</files>
  <read_first>src-tauri/src/tray.rs, src-tauri/icons/icon.png, src-tauri/Cargo.toml, .planning/phases/01-native-overlay-activation/01-UAT.md</read_first>
  <action>Load the existing `src-tauri/icons/icon.png` with Tauri's supported image-from-bytes API at compile time, pass that image to the current `TrayIconBuilder` through `.icon(...)`, and mark it as a template on macOS with `.icon_as_template(cfg!(target_os = "macos"))` so the status item remains legible across menu-bar appearances. Preserve the existing menu entries, controller callbacks, tooltip, startup-hidden behavior, `skipTaskbar` window behavior, and explicit Quit semantics exactly as they implement D-01, D-02, and D-04. Do not replace the checked-in asset or introduce a second tray lifecycle; handle image decoding through the existing Tauri `Result` path so a malformed asset fails during app setup instead of silently creating an unidentifiable tray item.</action>
  <verify>
    <automated>cargo fmt --manifest-path src-tauri/Cargo.toml -- --check &amp;&amp; cargo check --manifest-path src-tauri/Cargo.toml &amp;&amp; cargo test --manifest-path src-tauri/Cargo.toml</automated>
    <automated>test -s src-tauri/icons/icon.png &amp;&amp; rg -n 'include_bytes|TrayIconBuilder|\.icon\(|icon_as_template' src-tauri/src/tray.rs</automated>
  </verify>
  <done>The native tray builder consumes the existing PNG, requests macOS template rendering, and all Rust checks pass while the tray menu/controller wiring remains intact.</done>
</task>

<task type="auto">
  <name>Task 2: Document a reproducible debug launch and tray-only review path</name>
  <files>docs/support-matrix.md</files>
  <read_first>docs/support-matrix.md, package.json, src-tauri/tauri.conf.json, src-tauri/src/main.rs, .planning/phases/01-native-overlay-activation/01-UAT.md</read_first>
  <action>Add a Manual debug launch section to the Phase 1 support matrix. Tell the reviewer to run `pnpm tauri:dev` from the repository root, keep the terminal process attached, look for the nABrush status item in the macOS menu bar or Windows notification area, use its Show/Hide menu, and then invoke Cmd/Ctrl+Shift+A from a focused second application. State that the overlay and settings windows intentionally start hidden and do not create a normal document window; closing those windows hides them and only the tray Quit action exits, per D-01 and D-04. Explain that `src-tauri/target/debug/nabrush` is the WebDriver/debug executable path used by automation and does not provide a macOS app-bundle/Dock identity for manual UAT, so the documented `pnpm tauri:dev` path is the review entry point. Add an evidence checklist for tray icon visibility, cross-application shortcut activation, and the observed app/process state, while keeping the existing support limitations and device rows honest.</action>
  <verify>
    <automated>test -f docs/support-matrix.md &amp;&amp; rg -q 'Manual debug launch' docs/support-matrix.md &amp;&amp; rg -q 'pnpm tauri:dev' docs/support-matrix.md &amp;&amp; rg -q 'target/debug/nabrush' docs/support-matrix.md &amp;&amp; rg -q 'Cmd/Ctrl\\+Shift\\+A' docs/support-matrix.md</automated>
    <human-check>Run `pnpm tauri:dev` on macOS, confirm a visible nABrush status-item icon and its Show/Hide menu, focus another app, press Cmd+Shift+A, and record whether the overlay appears. Repeat the same documented flow on Windows with Ctrl+Shift+A when a Windows device is available.</human-check>
  </verify>
  <done>The support matrix gives a reviewer one explicit debug launch path, explains the expected tray-only lifecycle, and provides a place to record evidence for G-01-1 without changing intentional window behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checked-in PNG → native tray registration | A repository asset becomes a native status-item resource at compile time. |
| Debug process → menu-bar/system-tray shell | The running Tauri process exposes lifecycle controls outside the webview. |
| Manual reviewer → support evidence | Human observations are recorded as platform-specific acceptance evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-21 | Tampering | `src-tauri/icons/icon.png` and tray setup | medium | mitigate | Consume the checked-in asset through compile-time bytes, fail setup if Tauri cannot decode it, and verify the file plus builder wiring in the Rust validation command. |
| T-01-22 | Spoofing | Manual debug launch instructions | low | mitigate | Name the repository-root `pnpm tauri:dev` entry point and distinguish it from the automation-only raw executable path so evidence is tied to the intended nABrush process. |
| T-01-SC | Tampering | Package-manager dependencies | low | accept | This gap plan adds no package dependency or install step; the already-reviewed Phase 1 toolchain remains unchanged. |
</threat_model>

<verification>
<automated>cargo fmt --manifest-path src-tauri/Cargo.toml -- --check &amp;&amp; cargo check --manifest-path src-tauri/Cargo.toml &amp;&amp; cargo test --manifest-path src-tauri/Cargo.toml</automated>
<automated>pnpm exec tauri build --debug</automated>
<automated>pnpm exec wdio run wdio.conf.ts --suite short-lifecycle</automated>
<automated>test -f docs/support-matrix.md &amp;&amp; rg -q 'Manual debug launch' docs/support-matrix.md &amp;&amp; rg -q 'pnpm tauri:dev' docs/support-matrix.md</automated>
<human-check>Complete the macOS tray icon and cross-application activation evidence row using `pnpm tauri:dev`; complete the Windows row when a Windows device is available.</human-check>
</verification>

<success_criteria>
UAT gap G-01-1 is closable: the built macOS app has a discoverable status-item icon backed by `src-tauri/icons/icon.png`, the existing tray menu still controls the hidden overlay lifecycle, and a reviewer can reproduce the launch and shortcut checks from the documented debug path.
</success_criteria>

<output>
Create `.planning/phases/01-native-overlay-activation/01-08-SUMMARY.md` when done.
</output>
