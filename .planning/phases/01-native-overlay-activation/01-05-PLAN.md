---
phase: 01-native-overlay-activation
plan: 05
type: execute
wave: 5
depends_on: [01-04]
files_modified:
  - src-tauri/src/platform/mod.rs
  - src-tauri/src/platform/macos.rs
  - src-tauri/src/platform/windows.rs
  - src-tauri/src/controller.rs
  - src-tauri/src/main.rs
  - src-tauri/Cargo.toml
  - src/components/ModeBadge.tsx
  - src/components/mode-badge.test.tsx
  - src/App.tsx
  - src/styles.css
autonomous: true
requirements: [OVLY-03]
estimate:
  tokens: 21000
  raw_tokens: 14000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Interactive mode captures pointer input and click-through mode passes pointer input to the underlying app on the existing overlay window (D-09)."
    - "The mode badge and cursor feedback are visible, temporary, and separate from the retained annotation scene (D-08)."
  artifacts:
    - path: "src-tauri/src/platform/macos.rs"
      provides: "AppKit hit-testing and Space/full-screen policy"
      contains: "ignoresMouseEvents"
    - path: "src-tauri/src/platform/windows.rs"
      provides: "Win32 layered-window hit-testing policy"
      contains: "HTTRANSPARENT"
    - path: "src/components/ModeBadge.tsx"
      provides: "Mode feedback UI isolated from scene rendering"
      contains: "Click-through"
  key_links:
    - from: "src-tauri/src/controller.rs"
      to: "src-tauri/src/platform/mod.rs"
      via: "the registered controller invokes the target-gated adapter for each serialized mode transition"
      pattern: "PlatformWindowAdapter"
    - from: "src/App.tsx"
      to: "src/components/ModeBadge.tsx"
      via: "typed native mode events drive the feedback layer"
      pattern: "ModeBadge"
---

<objective>
Apply the explicit mode state to native hit testing on macOS and Windows and expose low-obstruction feedback. This satisfies D-08: mode badge and cursor feedback, and D-09: atomic click-through with shortcuts active.

Purpose: Make click-through reliable at the native boundary while keeping the mode visible to a presenter and outside the retained scene.
Output: Platform adapters, cursor policy, and tested mode badge.
</objective>

<execution_context>
@/Users/khacnghia/.codex/gsd-core/workflows/execute-plan.md
@/Users/khacnghia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-native-overlay-activation/01-CONTEXT.md
@.planning/phases/01-native-overlay-activation/01-03-PLAN.md
@.planning/phases/01-native-overlay-activation/01-04-PLAN.md
@.planning/phases/01-native-overlay-activation/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement native AppKit and Win32 hit-testing adapters</name>
  <files>src-tauri/src/platform/mod.rs, src-tauri/src/platform/macos.rs, src-tauri/src/platform/windows.rs, src-tauri/src/controller.rs, src-tauri/src/main.rs, src-tauri/Cargo.toml</files>
  <behavior>
    - Test 1: Interactive mode returns capture-pointer intent and preserves focus on the overlay.
    - Test 2: Click-through returns pass-through intent without changing window identity.
    - Test 3: The adapter contract is idempotent and does not recreate the native window.
  </behavior>
  <read_first>src-tauri/src/controller.rs, src-tauri/src/tracer.rs, .planning/phases/01-native-overlay-activation/01-RESEARCH.md, .planning/research/ARCHITECTURE.md</read_first>
  <action>Define `PlatformWindowAdapter` methods for show, hide, focusability, cursor-event handling, and retry. Register the platform module in the existing `main.rs`, inject the adapter into the one `AppController`, and invoke it from every controller mode transition; do not leave the adapter as an uncalled utility. Add the target-gated `windows` Rust dependency to `src-tauri/Cargo.toml` using the version approved by Plan 01 and only the Win32/Graphics Capture features required for hit testing. On macOS switch `NSWindow.ignoresMouseEvents` on the existing transparent window and apply the selected `fullScreenAuxiliary`/`canJoinAllApplications` policy; on Windows retain `WS_EX_LAYERED`, `WS_EX_TOOLWINDOW`, and `WS_EX_NOACTIVATE`, adjust the existing HWND's pass-through style, and return `HTTRANSPARENT` where appropriate. Keep global shortcut delivery independent of native hit testing, flush pointer capture during the transition, and leave multi-monitor topology to Phase 2. Gate OS-specific code with target modules and expose a deterministic adapter contract for host tests.</action>
  <acceptance_criteria>Platform tests cover interactive and click-through intents, adapter idempotence, existing-window reuse, controller registration/invocation, and target-gated compilation with the feature-gated Windows dependency; native code never creates a second overlay during a mode change.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml platform</automated>
    <fails_when>Platform adapter tests fail or macOS/Windows target-gated code cannot compile under the pinned toolchain.</fails_when>
  </verify>
  <done>Both supported platforms have an explicit native hit-testing adapter ready for the controller transitions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render and test mode badge and cursor feedback</name>
  <files>src/components/ModeBadge.tsx, src/components/mode-badge.test.tsx, src/App.tsx, src/styles.css</files>
  <behavior>
    - Test 1: VisibleInteractive renders a readable Drawing badge and drawing cursor treatment.
    - Test 2: VisibleClickThrough renders a readable Click-through badge and pass-through cursor treatment.
    - Test 3: The badge auto-hides after the documented short duration and is not added to the scene store.
  </behavior>
  <read_first>src/App.tsx, src/types/overlay.ts, src/state/overlay.ts, .planning/phases/01-native-overlay-activation/01-CONTEXT.md</read_first>
  <action>Subscribe the React surface to the typed native mode event and render a small corner `ModeBadge` for Hidden, Drawing, and Click-through. Use a two-second auto-hide timer while retaining current mode in the tray/settings state; use `crosshair` or equivalent drawing cursor treatment in interactive mode and a pass-through cursor treatment in click-through. Keep the badge and cursor classes in a UI layer separate from the canvas/scene data so later export composition excludes them. Never dim or tint an empty overlay.</action>
  <acceptance_criteria>Component tests prove readable mode-specific feedback, auto-hide, cursor treatment, and scene/export separation; mode updates originate from native events and hidden state removes overlay pixels.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/components/mode-badge.test.tsx</automated>
    <fails_when>Mode badge rendering, auto-hide, cursor feedback, or scene separation tests fail.</fails_when>
    <automated>pnpm exec tsc --noEmit</automated>
    <fails_when>Typed event subscriptions or UI styles fail strict compilation.</fails_when>
    <human-check>On macOS and Windows, show the overlay from another app, confirm the corner badge identifies Drawing and Click-through, observe the cursor treatment in each mode, and confirm the badge does not become a scene annotation.</human-check>
  </verify>
  <done>Users receive immediate but unobtrusive feedback for both pointer modes without contaminating the retained scene.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust controller → AppKit/Win32 | Mode changes alter native focus and hit testing |
| Rust mode event → React feedback | Native state crosses IPC into an informational UI layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Denial of service | click-through transition | high | mitigate | Apply a serialized, idempotent native transition, flush pointer capture, and keep fixed Esc registration active. |
| T-01-13 | Tampering | platform flags | medium | mitigate | Centralize OS-specific behavior behind `PlatformWindowAdapter` and test desired hit-test intent per target. |
| T-01-14 | Spoofing | mode badge | low | accept | Badge is informational and only renders typed controller events; it has no native authority. |
</threat_model>

<verification>
<automated>cargo test --manifest-path src-tauri/Cargo.toml platform</automated>
<fails_when>Platform adapter tests, controller invocation tests, or target-gated compilation fail under the pinned native dependency set.</fails_when>
<automated>pnpm exec vitest run src/components/mode-badge.test.tsx</automated>
<fails_when>Mode badge, auto-hide, cursor treatment, or scene-separation tests fail or are not discovered.</fails_when>
<automated>pnpm exec tsc --noEmit</automated>
<fails_when>Typed native mode events or platform feedback UI fail strict TypeScript compilation.</fails_when>
<human-check>On macOS and Windows, click-through a focused underlying app, confirm pointer delivery and global shortcut delivery, then switch back to Drawing.</human-check>
</verification>

<success_criteria>
Native hit testing and user feedback stay synchronized across both platforms, with one reusable window and an export-separate badge layer.
</success_criteria>

<output>
Create `.planning/phases/01-native-overlay-activation/01-05-SUMMARY.md` when done.
</output>
