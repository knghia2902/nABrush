---
phase: 01-native-overlay-activation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - vite.config.ts
  - vitest.config.ts
  - index.html
  - rust-toolchain.toml
  - src-tauri/Cargo.toml
  - src-tauri/Cargo.lock
autonomous: false
requirements: [OVLY-01, OVLY-02, OVLY-03, OVLY-04]
estimate:
  tokens: 18000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "From a clean checkout, the reviewed frontend scaffold installs and its TypeScript test and build checks complete successfully (D-01, D-02)."
    - "From a clean checkout, the pinned Rust toolchain resolves the Tauri manifest and the initial native build inputs are ready for the hidden overlay tracer (D-01, D-11)."
  artifacts:
    - path: "package.json"
      provides: "Pinned frontend, Tauri, Vitest, and build scripts"
      contains: "vitest"
    - path: "rust-toolchain.toml"
      provides: "Exact Rust channel and components for reproducible native builds"
      contains: "channel"
    - path: "vitest.config.ts"
      provides: "Explicit Vitest test discovery and Vite-compatible test environment"
      contains: "defineConfig"
    - path: "src-tauri/Cargo.lock"
      provides: "Committed native dependency resolution, including target-gated Windows crates"
      contains: "tauri"
    - path: "src-tauri/Cargo.toml"
      provides: "Pinned Tauri native manifest with target-gated Windows adapter dependency"
      contains: "tauri-build"
  key_links:
    - from: "package.json"
      to: "pnpm-lock.yaml"
      via: "package manager resolves the reviewed versions"
      pattern: "lockfile"
    - from: "rust-toolchain.toml"
      to: "src-tauri/Cargo.toml"
      via: "native build uses the pinned compiler"
      pattern: "toolchain"
---

<objective>
Prepare a reproducible Tauri 2, React/TypeScript, and Rust foundation for the Phase 1 overlay harness. The scaffold honors D-01: background launch with a hidden overlay, D-02: the Cmd/Ctrl+Shift+A default, and D-11: primary-display-only scope.

Purpose: Give every later slice a verified dependency set and an exact toolchain while preserving the local-first, tray utility scope.
Output: Frontend scaffold metadata, Vitest test command, and pinned Rust toolchain. The primary window contract is recorded for D-12: one transparent borderless topmost surface, D-13: show enters drawing mode, and D-14: an empty overlay stays transparent.
</objective>

<execution_context>
@/Users/khacnghia/.codex/gsd-core/workflows/execute-plan.md
@/Users/khacnghia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-native-overlay-activation/01-CONTEXT.md
@.planning/phases/01-native-overlay-activation/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@AGENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify all initial dependency identities before installation</name>
  <files>none</files>
  <read_first>.planning/phases/01-native-overlay-activation/01-RESEARCH.md</read_first>
  <action>Before any install, verify the native and frontend toolchain preflight with `command -v rustup`, `command -v cargo`, `command -v rustc`, and `command -v pnpm`. If Rust tooling is absent, install it through the official rustup path, add the pinned stable channel with `rustfmt` and `clippy`, and record the resulting `rustup show active-toolchain`, `rustc --version`, and `cargo --version` output before resuming. Inspect the official npm pages for `vite@8.2.2`, `@vitejs/plugin-react@6.1.1`, `@types/react-dom@19.2.7`, and `vitest@5.0.0`, plus crates.io pages for `tauri`, `tauri-build`, `tauri-plugin-global-shortcut`, and the target-gated `windows` crate required by Plan 05. Record each package/crate name, exact approved version, maintainer or source repository, registry URL, and intended role in the execution log. The Rust entries were unavailable during research and therefore require the same blocking review as the flagged npm entries. Approve exact versions or record verified replacements before any package manager or Cargo installation proceeds.</action>
  <acceptance_criteria>Rustup, rustc, cargo, pnpm, and the required Rust/npm registries are available and their versions are recorded; the SUS npm packages plus every native crate required by later plans have a human-verified registry identity, source, and approved version before installation.</acceptance_criteria>
  <verify>
    <automated>command -v rustup && command -v rustc && command -v cargo && command -v pnpm</automated>
    <fails_when>Any required Rust or pnpm executable is absent, so the dependent native scaffold cannot be reproduced.</fails_when>
    <automated>test -f .planning/phases/01-native-overlay-activation/01-RESEARCH.md</automated>
    <fails_when>The research audit or package list is unavailable for the legitimacy review.</fails_when>
    <human-check>Verify the four flagged npm pages and all four crates.io pages, install or confirm the official Rust toolchain when preflight fails, then record executable versions, registry evidence, approval, and exact versions in the execution log.</human-check>
  </verify>
  <resume-signal>Toolchain preflight and dependency legitimacy approval are recorded; resume with Task 2 to install the reviewed frontend and test dependencies.</resume-signal>
  <what-built>None; this checkpoint authorizes the reviewed dependency installation.</what-built>
  <how-to-verify>Confirm the execution log names all eight reviewed registry entries, their source repositories, approved versions, and the Rust/pnpm executable versions.</how-to-verify>
  <done>The blocking legitimacy gate and toolchain preflight have passed for every dependency required by Plans 01 through 05.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the pinned frontend and native build toolchain</name>
  <files>package.json, pnpm-lock.yaml, tsconfig.json, vite.config.ts, vitest.config.ts, index.html, rust-toolchain.toml, src-tauri/Cargo.toml, src-tauri/Cargo.lock</files>
  <read_first>.planning/research/STACK.md, .planning/phases/01-native-overlay-activation/01-RESEARCH.md, AGENTS.md</read_first>
  <action>Initialize the manual pnpm scaffold with the reviewed React 19, TypeScript, Vite, Tauri CLI, `@tauri-apps/api`, and global-shortcut versions. Add the reviewed `vitest@5.0.0` dependency and scripts for `dev`, `build`, `typecheck`, `test`, `tauri:dev`, and `tauri:build`; pin package-manager metadata and commit the generated lockfile. Configure strict TypeScript and the Tauri-compatible Vite dev/build contract, then create `vitest.config.ts` with explicit test discovery and the same alias/environment assumptions. Add `rust-toolchain.toml` with the exact approved stable Rust channel, `rustfmt`, `clippy`, and the host targets required by the project; record the channel in the file rather than relying on an ambient compiler. Create the initial `src-tauri/Cargo.toml` with the approved Tauri, tauri-build, and global-shortcut crates plus the target-gated `windows` crate and only the reviewed Win32/Graphics Capture features required by Plan 05. Generate `src-tauri/Cargo.lock` after that complete manifest is in place, verify it is committed, and keep native dependency resolution reproducible. Keep the scaffold local-only and free of capture, cloud, account, persistence, or billing services, satisfying D-01 and D-02's reproducible foundation.</action>
  <acceptance_criteria>All listed files define a reproducible React/TypeScript/Vite/Tauri scaffold, the explicit Vitest config is installed and runnable, `src-tauri/Cargo.toml` contains the reviewed target-gated Windows dependency, `src-tauri/Cargo.lock` records the complete native resolution after that dependency change, and the Rust compiler channel is pinned exactly with no out-of-scope service dependency. The checkpoint's package-legitimacy review remains recorded as execution evidence.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run --passWithNoTests</automated>
    <fails_when>The reviewed dependency lockfile cannot install or Vitest cannot start with the strict project configuration.</fails_when>
    <automated>pnpm exec tsc --noEmit</automated>
    <fails_when>TypeScript cannot resolve the pinned scaffold or strict compilation reports an error.</fails_when>
    <automated>cargo metadata --manifest-path src-tauri/Cargo.toml --locked --no-deps</automated>
    <fails_when>The pinned Rust manifest cannot resolve against the committed Cargo.lock, including the target-gated Windows dependency.</fails_when>
  </verify>
  <done>The dependency and toolchain foundation is reproducible and ready for the native tracer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registries → workspace | Third-party packages and native crates enter the build |
| local frontend → later Tauri IPC | The scaffold will become a privileged desktop client |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | package installation | high | mitigate | Human legitimacy review covers flagged npm packages and all unverified Cargo crates; lock versions in pnpm and Cargo metadata. |
| T-01-02 | Elevation of privilege | frontend scaffold | medium | mitigate | Keep capabilities and native commands out of the scaffold until the explicit Tauri capability file is added in Plan 02. |
</threat_model>

<verification>
<automated>pnpm exec vitest run --passWithNoTests</automated>
<fails_when>Vitest cannot start from the reviewed lockfile or the empty test run returns a non-zero status.</fails_when>
<automated>pnpm exec tsc --noEmit</automated>
<fails_when>Strict TypeScript compilation cannot resolve the pinned scaffold or returns a type error.</fails_when>
<automated>cargo metadata --manifest-path src-tauri/Cargo.toml --locked --no-deps</automated>
<fails_when>The committed Cargo.lock does not resolve the scaffold manifest and its target-gated Windows dependency.</fails_when>
</verification>

<success_criteria>
The reviewed dependency set installs reproducibly, Vitest and TypeScript checks run, and `rust-toolchain.toml` pins the compiler used by native plans.
</success_criteria>

<output>
Create `.planning/phases/01-native-overlay-activation/01-01-SUMMARY.md` when done.
</output>
