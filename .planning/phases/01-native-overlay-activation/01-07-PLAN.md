---
phase: 01-native-overlay-activation
plan: 07
type: execute
wave: 7
depends_on: [01-06]
files_modified:
  - wdio.conf.ts
  - tests/e2e/overlay.e2e.ts
  - package.json
  - pnpm-lock.yaml
  - .github/workflows/phase1.yml
  - docs/support-matrix.md
autonomous: false
requirements: [OVLY-01, OVLY-02, OVLY-03, OVLY-04]
estimate:
  tokens: 24000
  raw_tokens: 16000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The built app has an executable lifecycle smoke path for tray launch, global activation, mode switching, Esc hide, and scene restoration (D-01, D-06, D-09, D-10)."
    - "macOS 13+ and Windows 10 22H2+ have separate CI build/test jobs and an explicit real-device acceptance matrix."
    - "The support matrix records the macOS private-API/direct-distribution constraint and full-screen limitations without claiming Phase 2 or later features."
  artifacts:
    - path: "tests/e2e/overlay.e2e.ts"
      provides: "Built-app lifecycle and recovery smoke scenario"
      contains: "phase1-sentinel"
    - path: ".github/workflows/phase1.yml"
      provides: "Separate macOS and Windows validation jobs"
      contains: "macos"
    - path: "docs/support-matrix.md"
      provides: "Versioned platform support and manual evidence table"
      contains: "macOS 13"
  key_links:
    - from: "wdio.conf.ts"
      to: "tests/e2e/overlay.e2e.ts"
      via: "the Tauri WebDriver runner launches the built app and executes the lifecycle scenario"
      pattern: "tauri"
    - from: ".github/workflows/phase1.yml"
      to: "wdio.conf.ts"
      via: "both OS jobs run the same smoke contract on their native runner"
      pattern: "wdio"
    - from: "docs/support-matrix.md"
      to: "01-RESEARCH.md"
      via: "documented versions and distribution assumptions match resolved research"
      pattern: "10 22H2"
---

<objective>
Validate the Phase 1 lifecycle on the built application and record the exact macOS/Windows support contract.

Purpose: Exercise native behavior that unit tests cannot prove and leave an explicit CI and device path for future phase gates.
Output: WebDriver smoke suite, macOS/Windows CI workflow, and support matrix.
</objective>

<execution_context>
@/Users/khacnghia/.codex/gsd-core/workflows/execute-plan.md
@/Users/khacnghia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-native-overlay-activation/01-CONTEXT.md
@.planning/phases/01-native-overlay-activation/01-RESEARCH.md
@.planning/phases/01-native-overlay-activation/01-VALIDATION.md
@.planning/phases/01-native-overlay-activation/01-06-PLAN.md
@.planning/research/PITFALLS.md
@AGENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify WebDriver package legitimacy before smoke installation</name>
  <files>none</files>
  <read_first>.planning/phases/01-native-overlay-activation/01-RESEARCH.md, package.json</read_first>
  <action>Before adding desktop smoke dependencies, inspect the official npm pages for `@wdio/cli`, `@wdio/local-runner`, `@wdio/mocha-framework`, `@wdio/spec-reporter`, and `@wdio/tauri-service`. Record package ownership, source repositories, exact approved versions, and compatibility with the installed Tauri 2 CLI in the execution log. These packages are flagged SUS in the research audit, so approve exact versions or verified replacements before installation and pin them in the lockfile.</action>
  <acceptance_criteria>All five flagged WebDriver packages have a human-verified npm identity, source, and approved version before installation.</acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/01-native-overlay-activation/01-RESEARCH.md</automated>
    <fails_when>The package audit or smoke dependency list is unavailable for review.</fails_when>
    <human-check>Verify all five npm package pages, approve the versions, and record the registry evidence in the execution log.</human-check>
  </verify>
  <resume-signal>WebDriver legitimacy approval is recorded; resume with Task 2 to install the reviewed smoke packages.</resume-signal>
  <what-built>None; this checkpoint authorizes the reviewed smoke-test toolchain.</what-built>
  <how-to-verify>Confirm the execution log names each reviewed package, repository, and exact version.</how-to-verify>
  <done>The blocking legitimacy gate has passed for every WebDriver dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the built-app lifecycle smoke scenario</name>
  <files>wdio.conf.ts, tests/e2e/overlay.e2e.ts, package.json, pnpm-lock.yaml, docs/support-matrix.md</files>
  <behavior>
    - Test 1: A cold launch exposes tray availability and no normal document window.
    - Test 2: Cmd/Ctrl+Shift+A from a focused second app shows interactive mode, and the settings replacement binding is invoked successfully.
    - Test 3: The WebDriver smoke path leaves the overlay visible and keeps the global shortcut service active after click-through; the device matrix separately verifies pointer delivery to a concrete underlying app fixture.
    - Test 4: The test adds `phase1-sentinel`, presses Esc, and confirms Show restores the same scene record.
    - Test 5: The full `phase1-matrix` gate suite uses deterministic injected conflict and initialization-failure fixtures to prove rollback and Retry state when OS injection is unavailable.
  </behavior>
  <read_first>package.json, src-tauri/src/controller.rs, src-tauri/src/shortcut.rs, src/components/SettingsPanel.tsx, src/components/ErrorBadge.tsx, .planning/phases/01-native-overlay-activation/01-VALIDATION.md</read_first>
  <action>Install only the versions approved by Task 1 and configure WebdriverIO's Tauri service to launch the built app. Define a short `short-lifecycle` suite for rapid feedback that observes tray launch, invokes the configurable visibility binding from another app, verifies Drawing and Click-through state transitions and shortcut continuity, presses bare Esc, and confirms the `phase1-sentinel` scene record survives restoration. Define the complete `phase1-matrix` suite separately for the later wave/phase gate, where the longer recovery and platform checks run after the short suite is green. Keep WebDriver assertions inside the built app and webview boundary. For pointer delivery, declare a concrete device fixture in `docs/support-matrix.md`: macOS TextEdit with a known text target and Windows Notepad with a known text target; the operator clicks that target while click-through is active and records whether the underlying app received the click. Add deterministic command fixtures for conflict rollback and initialization failure so the test can assert typed error/retry state without faking OS hit testing. Keep both suites within primary-display activation and the Phase 1 boundary.</action>
  <acceptance_criteria>The short smoke suite executes against a built Tauri app and covers background launch, configurable activation, native modes, click-through shortcut continuity, and Esc scene restoration; the full `phase1-matrix` gate adds conflict rollback and initialization retry; the support matrix names the TextEdit and Notepad pointer-delivery fixtures for native device verification.</acceptance_criteria>
  <verify>
    <automated>pnpm exec wdio run wdio.conf.ts --suite short-lifecycle</automated>
    <fails_when>The short lifecycle suite cannot launch the built app, any activation/mode/Esc/scene assertion fails, or the configured target runner is not exercised.</fails_when>
    <automated>pnpm exec tauri build --debug</automated>
    <fails_when>The debug artifact cannot be rebuilt after smoke-test dependencies and configuration are added.</fails_when>
  </verify>
  <done>The built-app lifecycle has a repeatable automated smoke path on native runners.</done>
</task>

<task type="auto">
  <name>Task 3: Add explicit macOS and Windows CI and support evidence</name>
  <files>.github/workflows/phase1.yml, docs/support-matrix.md</files>
  <read_first>.planning/phases/01-native-overlay-activation/01-RESEARCH.md, .planning/phases/01-native-overlay-activation/01-VALIDATION.md, .planning/ROADMAP.md, .planning/research/PITFALLS.md</read_first>
  <action>Define separate GitHub Actions jobs on `macos-13` and `windows-2022` that install the pinned Node/pnpm/Rust toolchains, run TypeScript and Rust unit tests, build the debug app, and expose the short WebDriver smoke command for rapid feedback on each native runner. Keep the complete `phase1-matrix` WebDriver suite as an explicit wave/phase-gate command after the short suite and unit/native checks pass; its longer runtime must never block task-level feedback. Document the support matrix for macOS 13 Ventura or newer and Windows 10 22H2 or newer with WebView2, including tray lifecycle, primary-display transparency, activation from another app, interactive capture, click-through, fixed Esc, settings close-to-hide, conflict rollback, initialization retry, macOS Spaces/Stage Manager/native full-screen observations, Windows borderless versus exclusive full-screen behavior, and the `macOSPrivateApi` direct signed/notarized distribution consequence. Record pass/fail, OS version, and evidence location for physical devices; identify exclusive full-screen and unavailable permission outcomes as limitations. Keep multi-monitor topology, drawing tools, capture/export, persistence, and billing outside this matrix.</action>
  <acceptance_criteria>CI contains explicit macOS and Windows paths with the same test commands, and the support matrix records versioned automated/device evidence plus the private-API and full-screen limitations without overclaiming support.</acceptance_criteria>
  <verify>
    <automated>pnpm exec wdio run wdio.conf.ts --suite phase1-matrix</automated>
    <fails_when>The explicit Phase 1 gate suite cannot launch the built app or any long-running lifecycle, mode, shortcut, scene, conflict, or retry assertion fails on the target runner.</fails_when>
    <automated>test -f .github/workflows/phase1.yml && test -f docs/support-matrix.md && rg -q "macos-13" .github/workflows/phase1.yml && rg -q "windows-2022" .github/workflows/phase1.yml</automated>
    <fails_when>Either platform job, support baseline, or direct-distribution/private-API note is missing.</fails_when>
    <human-check>Run the matrix on one macOS 13+ device and one Windows 10 22H2+ device, record results and evidence in docs/support-matrix.md, and include bare Esc, click-through, close-to-hide, and recovery checks.</human-check>
  </verify>
  <done>Phase 1 has a concrete macOS/Windows execution path and an honest, versioned support contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → smoke toolchain | WebDriver dependencies enter the test environment |
| WebDriver → built desktop process | Automation drives native lifecycle and input surfaces |
| CI runner/device → support evidence | Platform behavior is recorded for release decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-18 | Tampering | WebDriver dependencies | high | mitigate | Pass the blocking legitimacy checkpoint and pin reviewed versions in the lockfile. |
| T-01-19 | Denial of service | smoke harness | medium | mitigate | Use deterministic fixtures for injected failures and fail closed in the app; do not bypass native recovery assertions. |
| T-01-20 | Information disclosure | support matrix | low | accept | Record platform versions and error codes without screen pixels, credentials, or user content. |
</threat_model>

<verification>
<automated>pnpm exec wdio run wdio.conf.ts --suite short-lifecycle</automated>
<fails_when>The rapid lifecycle suite cannot launch the built app or any activation, mode, Esc, or scene assertion fails.</fails_when>
<automated>pnpm exec wdio run wdio.conf.ts --suite phase1-matrix</automated>
<fails_when>The explicit Phase 1 gate matrix cannot launch the built app or any lifecycle, mode, shortcut, scene, conflict, or retry assertion fails on the target runner.</fails_when>
<automated>pnpm exec tauri build --debug</automated>
<fails_when>The debug artifact cannot be rebuilt with the smoke configuration and reviewed dependencies.</fails_when>
<automated>test -f .github/workflows/phase1.yml && test -f docs/support-matrix.md</automated>
<fails_when>Either native CI job, support baseline, or concrete pointer-delivery fixture is missing from the support evidence.</fails_when>
<human-check>Complete the macOS and Windows device rows in docs/support-matrix.md.</human-check>
</verification>

<success_criteria>
The built app passes the lifecycle smoke suite on explicit macOS and Windows runners, and the support matrix records the chosen OS baselines, distribution consequence, full-screen limitations, and device evidence.
</success_criteria>

<output>
Create `.planning/phases/01-native-overlay-activation/01-07-SUMMARY.md` when done.
</output>
