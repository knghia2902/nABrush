---
phase: 01-native-overlay-activation
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - src/types/overlay.ts
  - src/types/mode-schema.json
  - src/state/overlay.ts
  - src/state/overlay.test.ts
  - src-tauri/src/controller.rs
  - src-tauri/src/mode_schema.rs
  - src-tauri/src/main.rs
autonomous: true
requirements: [OVLY-02, OVLY-04]
estimate:
  tokens: 18000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Hidden, VisibleInteractive, and VisibleClickThrough are explicit, serialized, idempotent states that do not own or destroy the retained scene (D-06, D-09, D-10)."
    - "A deterministic annotation scene sentinel added before emergency hide is present after Show restores the same webview scene (D-10, D-17)."
  artifacts:
    - path: "src/state/overlay.ts"
      provides: "Pure renderer transition reducer and retained scene fixture"
      contains: "phase1-sentinel"
    - path: "src/state/overlay.test.ts"
      provides: "Transition, idempotence, and emergency scene-preservation tests"
      contains: "phase1-sentinel"
    - path: "src-tauri/src/controller.rs"
      provides: "Authoritative serialized native mode contract"
      contains: "VisibleClickThrough"
    - path: "src/types/mode-schema.json"
      provides: "Canonical cross-language mode and action vocabulary fixture"
      contains: "VisibleInteractive"
  key_links:
    - from: "src/state/overlay.ts"
      to: "src/state/overlay.test.ts"
      via: "the same scene fixture and mode vocabulary drive deterministic assertions"
      pattern: "phase1-sentinel"
    - from: "src-tauri/src/controller.rs"
      to: "src/state/overlay.ts"
      via: "native events serialize into the renderer's discriminated mode type"
      pattern: "OverlayMode"
    - from: "src/types/mode-schema.json"
      to: "src-tauri/src/mode_schema.rs"
      via: "Rust serialization checks consume the same canonical fixture as TypeScript"
      pattern: "include_str"
---

<objective>
Define and test the explicit mode contract while proving that emergency hide preserves the retained webview scene.

Purpose: Make visibility, pointer capture, click-through, and emergency recovery deterministic before native platform details and settings are added.
Output: Shared mode types, a tested renderer reducer with a scene sentinel, and a matching Rust controller.
</objective>

<execution_context>
@/Users/khacnghia/.codex/gsd-core/workflows/execute-plan.md
@/Users/khacnghia/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-native-overlay-activation/01-CONTEXT.md
@.planning/phases/01-native-overlay-activation/01-02-PLAN.md
@.planning/phases/01-native-overlay-activation/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test the mode reducer and retained scene sentinel</name>
  <files>src/types/overlay.ts, src/types/mode-schema.json, src/state/overlay.ts, src/state/overlay.test.ts</files>
  <behavior>
    - Test 1: Hidden plus ToggleVisibility enters VisibleInteractive and emits one show/interactive effect.
    - Test 2: VisibleInteractive plus ToggleClickThrough enters VisibleClickThrough while retaining the scene id.
    - Test 3: Add `phase1-sentinel`, invoke Esc emergency hide, then Show and assert the same sentinel remains in the scene.
    - Test 4: Repeating the current transition is idempotent and produces no duplicate window side effect.
    - Test 5: The TypeScript serialized mode/action arrays exactly match the canonical `mode-schema.json` fixture.
  </behavior>
  <read_first>src/App.tsx, .planning/phases/01-native-overlay-activation/01-CONTEXT.md, .planning/research/ARCHITECTURE.md</read_first>
  <action>Define serialized `OverlayMode`, `ShortcutAction`, `ModeState`, `SceneItem`, and typed event payloads. Add the canonical `mode-schema.json` fixture listing the exact mode and action strings; load it in the TypeScript test and compare the serialized arrays directly. Implement a pure transition reducer plus a long-lived webview scene store. The test fixture must add a deterministic annotation record with id `phase1-sentinel` before dispatching emergency hide, assert Hidden leaves the record untouched, and assert a later Show returns VisibleInteractive with that same record. Keep scene data in the Browser/Client tier assigned by the Architectural Responsibility Map; the reducer may return opaque scene identity for assertions but Rust must not own semantic annotation objects.</action>
  <acceptance_criteria>Vitest proves all three mode states, separate actions, idempotence, exact TypeScript-to-fixture vocabulary compatibility, and add-before-hide/restore-after-Show scene preservation using `phase1-sentinel`; no test relies on an undefined scene identifier.</acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/state/overlay.test.ts</automated>
    <fails_when>Any mode, idempotence, or sentinel-preservation assertion fails or the target test is not discovered.</fails_when>
  </verify>
  <done>The renderer contract and concrete emergency-hide fixture are executable and preserve the scene in the webview.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror the mode contract in the serialized Rust controller</name>
  <files>src-tauri/src/controller.rs, src-tauri/src/mode_schema.rs, src-tauri/src/main.rs, src/types/overlay.ts, src/types/mode-schema.json</files>
  <behavior>
    - Test 1: Hidden plus ToggleVisibility produces one Show plus Interactive effect.
    - Test 2: Click-through changes native hit-testing intent without changing the opaque scene reference.
    - Test 3: Esc from any visible mode produces Hidden and leaves the scene reference unchanged.
    - Test 4: Invalid actions and repeated state transitions do not recreate the window or mutate scene identity.
    - Test 5: Rust serialization of every mode and action matches the exact strings in the shared `mode-schema.json` fixture.
  </behavior>
  <read_first>src-tauri/src/tracer.rs, src-tauri/src/main.rs, src/state/overlay.ts, .planning/research/ARCHITECTURE.md</read_first>
  <action>Extend the authoritative Rust controller created and registered by Plan 02 with `Hidden`, `VisibleInteractive`, and `VisibleClickThrough`, serialized transition handling, typed events, and an opaque scene reference only. Update the existing `main.rs` registration and managed-state wiring explicitly so the expanded controller remains the one instance used by tray and shortcut callbacks; never introduce a second controller. Add `mode_schema.rs` and include the shared `src/types/mode-schema.json` fixture in a Rust test that serializes every mode and action and compares the exact vocabulary. Route Show, Hide, ToggleVisibility, ToggleClickThrough, Esc, and Retry through the same existing window identity. Keep Esc independently deliverable in click-through mode, reject invalid enum/payload values at the IPC boundary, and preserve the controller's scene reference across hide/show without moving semantic scene ownership out of React.</action>
  <acceptance_criteria>Rust tests pass for the three states, idempotence, invalid-action rejection, click-through intent, Esc scene-reference preservation, and direct Rust-to-fixture serialization compatibility; `main.rs` still registers exactly one controller and the serialized vocabulary matches TypeScript exactly.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml controller</automated>
    <fails_when>Controller tests fail, invalid actions mutate state, or Rust and TypeScript mode names diverge.</fails_when>
  </verify>
  <done>The native controller and webview reducer share a deterministic, scene-preserving mode contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri IPC → controller | Untrusted action payloads request native state transitions |
| native mode events → webview reducer | Rust state is reflected into renderer UI |
| renderer scene → lifecycle controller | Semantic annotation data must survive native hide/show |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Tampering | mode IPC | high | mitigate | Deserialize explicit enums, reject unknown actions, and keep transitions serialized and idempotent. |
| T-01-07 | Denial of service | emergency hide | high | mitigate | Preserve the existing window and opaque scene reference while forcing Hidden; keep Esc independent of hit testing. |
| T-01-08 | Information disclosure | scene fixture | low | accept | The sentinel is local test data; no screen pixels or remote state cross the boundary. |
</threat_model>

<verification>
<automated>pnpm exec vitest run src/state/overlay.test.ts</automated>
<fails_when>Reducer, scene-sentinel, idempotence, or TypeScript fixture-compatibility tests fail or are not discovered.</fails_when>
<automated>cargo test --manifest-path src-tauri/Cargo.toml controller</automated>
<fails_when>Controller, shared-schema, invalid-action, or scene-reference tests fail, or main.rs no longer wires the authoritative controller.</fails_when>
</verification>

<success_criteria>
The scene-preserving emergency path is proven with a concrete sentinel and the Rust controller cannot silently diverge from the renderer mode contract.
</success_criteria>

<output>
Create `.planning/phases/01-native-overlay-activation/01-03-SUMMARY.md` when done.
</output>
